# Quick installation steps

> **Index:** `12.2`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=installing-quick-installation-steps](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=installing-quick-installation-steps)

---

## Quick installation steps

Last Updated
: 2026-09-09

Use these instructions for a quick installation overview to set up a Ceph Storage cluster on virtual machines.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- At least three VMs or bare-metal servers with internet access.
- Red Hat Enterprise Linux
9.8 or 10.2
with
`ansible-core`
2.16.18 bundled into AppStream.

> 📝 **Note:** Note:
If you purchased an
IBM Storage Ceph
Premium license, complete the Red Hat entitlement activation process before beginning deployment.

For more information, see Accessing Red Hat entitlements from IBM Storage Ceph.
- Server and Ceph repositories must be enabled.
- Root-level access to all nodes.
- An active IBM Network or service account to access the IBM Registry.

> 📝 **Note:** Note:
For information about obtaining credentials for
`cp.icr.io/cp`
, see
Obtaining an entitlement key
.
- Remove troubling configurations in iptables so that refreshing the iptables service does not cause issues for the cluster. For an example, see Verifying firewall rules are configured for default Ceph ports.
- Review all the installation requirements. For more information, see Installation and upgrade requirements.
- sudo permissions are set.

### About this task

The instructions here are high-level installation instructions only.
Figure 1
illustrates the basic flow of installation. For full in-depth information and commands for each step, follow the procedures in the following sections or open the image in a new tab and click the steps for direct links to the relevant topics.
Figure 1.
Quick installation steps

### Procedure

1. Enable the necessary repositories on all nodes.

- Enabling repositories with Red Hat Enterprise Linux 9.x:

```plaintext-ibm
subscription-manager register
subscription-manager refresh
subscription-manager repos --disable=*
subscription-manager repos --enable=rhel-9-for-x86_64-baseos-rpms --enable=rhel-9-for-x86_64-appstream-rpms
dnf update
 curl https://public.dhe.ibm.com/ibmdl/export/pub/storage/ceph/ibm-storage-ceph-9-rhel-9.repo | sudo tee /etc/yum.repos.d/ibm-storage-ceph-9-rhel-9.repo
```

Copy to clipboard
- Enabling repositories with Red Hat Enterprise Linux 10.x:

```plaintext-ibm
subscription-manager register
subscription-manager refresh
subscription-manager repos --disable=*
subscription-manager repos --enable=rhel-10-for-x86_64-baseos-rpms --enable=rhel-10-for-x86_64-appstream-rpms
dnf update
 curl https://public.dhe.ibm.com/ibmdl/export/pub/storage/ceph/ibm-storage-ceph-9-rhel-10.repo | sudo tee /etc/yum.repos.d/ibm-storage-ceph-9-rhel-10.repo
```

Copy to clipboard

  - Enabling repositories with Red Hat Enterprise Linux 9.x:

  ```plaintext-ibm
  subscription-manager register
  subscription-manager refresh
  subscription-manager repos --disable=*
  subscription-manager repos --enable=rhel-9-for-x86_64-baseos-rpms --enable=rhel-9-for-x86_64-appstream-rpms
  dnf update
   curl https://public.dhe.ibm.com/ibmdl/export/pub/storage/ceph/ibm-storage-ceph-9-rhel-9.repo | sudo tee /etc/yum.repos.d/ibm-storage-ceph-9-rhel-9.repo
  ```



  Copy to clipboard
  - Enabling repositories with Red Hat Enterprise Linux 10.x:

  ```plaintext-ibm
  subscription-manager register
  subscription-manager refresh
  subscription-manager repos --disable=*
  subscription-manager repos --enable=rhel-10-for-x86_64-baseos-rpms --enable=rhel-10-for-x86_64-appstream-rpms
  dnf update
   curl https://public.dhe.ibm.com/ibmdl/export/pub/storage/ceph/ibm-storage-ceph-9-rhel-10.repo | sudo tee /etc/yum.repos.d/ibm-storage-ceph-9-rhel-10.repo
  ```



  Copy to clipboard
2. Add and accept the
IBM Storage Ceph
license on each node.
rhel

```plaintext-ibm
dnf install ibm-storage-ceph-license
touch /usr/share/ibm-storage-ceph-license/accept
```

Copy to clipboard

> 📝 **Note:** Important:
Starting from
IBM Storage Ceph
9.9.1
,
Call Home is enabled by default. When you accept the license, the Call Home feature is automatically enabled.

By using Call Home, you agree to allow IBM and its subsidiaries to store and use your contact information and certain system information anywhere you do business worldwide as described in the Program license agreement and documentation. For more information, refer to the Program license agreement and documentation.

For more information about Call Home and disabling after installation, see Call Home.
3. Install the cephadm-ansible package on the admin node.

```plaintext-ibm
dnf install cephadm-ansible
```

Copy to clipboard
4. Create an Ansible user with sudo access.

1. Enable SSH login as
root user
on Red Hat Linux Enterprise.

```plaintext-ibm
echo 'PermitRootLogin yes' >> /etc/ssh/sshd_config.d/01-permitrootlogin.conf
systemctl restart sshd.service
ssh root@HOST_NAME
```

Copy to clipboard

Replace
HOST_NAME
with the host name of the Ceph node.
2. Create an Ansible user with sudo privileges.

```plaintext-ibm
adduser ceph-admin
passwd ceph-admin
cat << EOF >/etc/sudoers.d/ceph-admin
ceph-admin ALL = (root) NOPASSWD:ALL
EOF
```

Copy to clipboard
3. Apply the Ansible user permissions.

```plaintext-ibm
chmod 0440 /etc/sudoers.d/ceph-admin
```

Copy to clipboard
4. Generate an SSH key pair on the
`cephadm`
node and configure SSH settings.

```plaintext-ibm
su ceph-admin
ssh-keygen
ssh-copy-id ceph-admin@host01
vi ~/.ssh/config
touch ~/.ssh/config
chmod 600 ~/.ssh/config
```

Copy to clipboard

  1. Enable SSH login as
  root user
  on Red Hat Linux Enterprise.

  ```plaintext-ibm
  echo 'PermitRootLogin yes' >> /etc/ssh/sshd_config.d/01-permitrootlogin.conf
  systemctl restart sshd.service
  ssh root@HOST_NAME
  ```



  Copy to clipboard

  Replace
  HOST_NAME
  with the host name of the Ceph node.
  2. Create an Ansible user with sudo privileges.

  ```plaintext-ibm
  adduser ceph-admin
  passwd ceph-admin
  cat << EOF >/etc/sudoers.d/ceph-admin
  ceph-admin ALL = (root) NOPASSWD:ALL
  EOF
  ```



  Copy to clipboard
  3. Apply the Ansible user permissions.

  ```plaintext-ibm
  chmod 0440 /etc/sudoers.d/ceph-admin
  ```



  Copy to clipboard
  4. Generate an SSH key pair on the
  `cephadm`
  node and configure SSH settings.

  ```plaintext-ibm
  su ceph-admin
  ssh-keygen
  ssh-copy-id ceph-admin@host01
  vi ~/.ssh/config
  touch ~/.ssh/config
  chmod 600 ~/.ssh/config
  ```



  Copy to clipboard
5. Prepare and run the preflight playbook.

The Ansible playbook configures the Ceph repository and prepares the storage cluster for bootstrapping. It also installs some prerequisites, such as
`podman`
,
`lvm2`
,
`chrony`
, and
`cephadm`
packages. The default location for
`cephadm-ansible`
and
cephadm-preflight.yml
is
/usr/share/cephadm-ansible
.

The preflight playbook uses the `cephadm-ansible` inventory file to identify the admin node and all other nodes in the storage cluster.

The default location for the inventory file is /usr/share/cephadm-ansible/hosts.

1. Add the configured nodes to the
hosts
file.
For example,

```plaintext-ibm
host02
host03
host04
[admin]
host01
```

Copy to clipboard
2. Run the preflight playbook.

```plaintext-ibm
ansible-playbook -i hosts cephadm-preflight.yml --extra-vars "ceph_origin=ibm"
```

Copy to clipboard

A
Preflight Check Report
and
Summary
generates and highlights passed, failed, and informational checks.

  1. Add the configured nodes to the
  hosts
  file.
  For example,

  ```plaintext-ibm
  host02
  host03
  host04
  [admin]
  host01
  ```



  Copy to clipboard
  2. Run the preflight playbook.

  ```plaintext-ibm
  ansible-playbook -i hosts cephadm-preflight.yml --extra-vars "ceph_origin=ibm"
  ```



  Copy to clipboard

  A
  Preflight Check Report
  and
  Summary
  generates and highlights passed, failed, and informational checks.
6. Bootstrap the cluster as either a root user or a non-root user.

- Bootstrap the cluster as a root user by running the `cephadm` boostrap on the initial monitor node.

            For example,

```plaintext-ibm
cephadm bootstrap --cluster-network 10.10.128.0/24 --mon-ip 10.10.128.68 --registry-url cp.icr.io/cp --registry-username myuser1 --registry-password mypassword1
```

Copy to clipboard

- Bootsrap the cluster as a non-root user by enabling passwordless sudo access.

            For example,

```plaintext-ibm
su - ceph-admin
ssh host01
cephadm bootstrap --ssh-user ceph-admin --mon-ip 10.10.128.68 --registry-url cp.icr.io/cp --registry-username myuser1 --registry-password mypassword1
```

Copy to clipboard

  - Bootstrap the cluster as a root user by running the `cephadm` boostrap on the initial monitor node.

              For example,

  ```plaintext-ibm
  cephadm bootstrap --cluster-network 10.10.128.0/24 --mon-ip 10.10.128.68 --registry-url cp.icr.io/cp --registry-username myuser1 --registry-password mypassword1
  ```



  Copy to clipboard

### What to do next

Your single-node (admin node or bootstrap node) Ceph cluster is now ready.For more information about how to use the `cephadm` orchestrator to perform "Day Two" operations, see Operations.

To deploy, configure, and administer the Ceph Object Gateway on Day Two operations, see Ceph Object Gateway.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=installing-quick-installation-steps](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=installing-quick-installation-steps)*