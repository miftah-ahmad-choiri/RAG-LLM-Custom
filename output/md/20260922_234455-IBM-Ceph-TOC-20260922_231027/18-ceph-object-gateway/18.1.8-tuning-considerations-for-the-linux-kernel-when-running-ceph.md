# Tuning considerations for the Linux kernel when running Ceph

> **Index:** `18.1.8`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=gateway-tuning-considerations-linux-kernel-when-running-ceph](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=gateway-tuning-considerations-linux-kernel-when-running-ceph)

---

## Tuning considerations for the Linux kernel when running Ceph

Last Updated
: 2026-09-06

Tune the operating system, specifically for limits and memory allocation.

Production IBM Storage clusters generally benefit from tuning the operating system, specifically around limits and memory allocation. Ensure that adjustments are set for all hosts within the storage cluster. For more guidance, contact [IBM Support](https://www.ibm.com/mysupport/).

> 📝 **Note:** Important:
Operating System swap must not be enabled. Configuring the operating system to use swap to provide additional virtual memory for daemons is not recommended for modern systems and can result in degraded performance.

### Increasing the file descriptors

The Ceph Object Gateway can hang if it runs out of file descriptors. You can modify the
/etc/security/limits.conf
file on Ceph Object Gateway hosts to increase the file descriptors for the Ceph Object Gateway.

```plaintext-ibm
ceph       soft    nofile     unlimited
```

Copy to clipboard

### Adjusting the ulimit value for large storage clusters

Create a
/etc/security/limits.d/50-ceph.conf
file on each host that runs administrative commands, when running Ceph administrative commands on large storage clusters. An example of a large storage cluster is one with 1024 or more Ceph OSDs.

> 📝 **Note:** Note:
The root user’s
`ulimit`
value is already set to
`unlimited`
by default on Red Hat Enterprise Linux.

Include the following contents to the configuration file:

```plaintext-ibm
USER_NAME       soft    nproc     unlimited
```

Copy to clipboard

Replace USER_NAME with the name of the non-root user account that runs the Ceph administrative commands.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=gateway-tuning-considerations-linux-kernel-when-running-ceph](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=gateway-tuning-considerations-linux-kernel-when-running-ceph)*