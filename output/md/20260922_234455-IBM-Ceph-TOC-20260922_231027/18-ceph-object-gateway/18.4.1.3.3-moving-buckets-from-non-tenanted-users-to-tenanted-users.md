# Moving buckets from non-tenanted users to tenanted users

> **Index:** `18.4.1.3.3`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=mb-moving-buckets-from-non-tenanted-users-tenanted-users](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=mb-moving-buckets-from-non-tenanted-users-tenanted-users)

---

## Moving buckets from non-tenanted users to tenanted users

Last Updated
: 2026-06-25

You can move buckets from a non-tenanted user to a tenanted user.

### Procedure

1. Optional: If you do not already have multiple tenants, you can create them by enabling
`rgw_keystone_implicit_tenants` and accessing the Ceph Object Gateway from an
external tenant:

Enable the `rgw_keystone_implicit_tenants` option:

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config set client.rgw rgw_keystone_implicit_tenants true
```

Copy to clipboard

Access the Ceph Object Gateway from an eternal tenant using either the `s3cmd` or
`swift` command:

Example

```plaintext-ibm
[ceph: root@host01 /]# swift list
```

Copy to clipboard

Or use `s3cmd`:

Example

```plaintext-ibm
[ceph: root@host01 /]# s3cmd ls
```

Copy to clipboard

The first access from an external tenant creates an equivalent Ceph Object Gateway user.
2. Move a bucket to a tenanted user:

Syntax

```plaintext-ibm
radosgw-admin bucket link --bucket=/BUCKET --uid=TENANT$USER
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# radosgw-admin bucket link --bucket=/data --uid='test$tenanted-user'
```

Copy to clipboard
3. Verify that the `data` bucket has been linked to `tenanted-user`
successfully:

Example

```plaintext-ibm
[ceph: root@host01 /]# radosgw-admin bucket list --uid='test$tenanted-user'
[
    "data"
]
```

Copy to clipboard
4. Change the ownership of the bucket to the new user:

Syntax

```plaintext-ibm
radosgw-admin bucket chown --bucket=tenant/bucket name --uid=tenant$user
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# radosgw-admin bucket chown --bucket='test/data' --uid='test$tenanted-user'
```

Copy to clipboard
5. Verify that the ownership of the `data` bucket has been successfully changed by
checking the `owner` line in the output of the following command:

Example

```plaintext-ibm
[ceph: root@host01 /]# radosgw-admin bucket list --bucket=test/data
```

Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=mb-moving-buckets-from-non-tenanted-users-tenanted-users](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=mb-moving-buckets-from-non-tenanted-users-tenanted-users)*