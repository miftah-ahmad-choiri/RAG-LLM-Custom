# Configuring lifecycle expiration window

> **Index:** `18.4.1.7.5`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=lifecycle-configuring-expiration-window](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=lifecycle-configuring-expiration-window)

---

## Configuring lifecycle expiration window

Last Updated
: 2026-06-25

You can set the time that the lifecycle management process runs each day by setting the
rgw_lifecycle_work_time parameter. By default, lifecycle processing occurs once
per day, at midnight.

### Prerequisites

- A running IBM Storage Ceph cluster.
- A Ceph Object Gateway node.
- Installation of the Ceph Object Gateway.
- Root-level access to a Ceph Object Gateway node.

### Procedure

1. Log into the `cephadm` shell:

Example

```plaintext-ibm
[root@host01 ~]# cephadm shell
```

Copy to clipboard
2. Set the lifecycle expiration time:

Syntax

```plaintext-ibm
ceph config set client.rgw rgw_lifecycle_work_time %D:%D-%D:%D
```

Copy to clipboard

Replace *%d:%d-%d:%d* with `start_hour:start_minute-end_hour:end_minute`.

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config set client.rgw rgw_lifecycle_work_time 06:00-08:00
```

Copy to clipboard

### Verification

- Retrieve the lifecycle expiration work time:

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config get client.rgw rgw_lifecycle_work_time

06:00-08:00
```

Copy to clipboard

### Reference

For more information, see the following:

- S3 bucket lifecycle

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=lifecycle-configuring-expiration-window](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=lifecycle-configuring-expiration-window)*