# Modify user information

> **Index:** `18.4.3.5`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=management-modify-user-information](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=management-modify-user-information)

---

## Modify user information

Last Updated
: 2026-09-06

Modify user information, by using the user modify command. Typically, modifications are made to keys and secrets, email addresses, display names, and access levels.

To modify information about a user, you must specify the user ID (`--uid=_USERNAME_`) and the attributes you want to modify.

Example

```plaintext-ibm
[root@host01 ~]# radosgw-admin user modify --uid=janedoe --display-name="Jane E. Doe"
```

Copy to clipboard

Example: Modify a user's default storage class and placement target.

```plaintext-ibm
[root@host01 ~]# radosgw-admin user modify --uid=user1 --storage-class=cold --placement-id=default-placement
```

Copy to clipboard

> 📝 **Note:** Note:
Ensure that the specified storage class and placement target exist in the cluster before modifying the user.

To modify subuser values, specify `subuser modify` and the subuser ID.

Example

```plaintext-ibm
[root@host01 ~]# radosgw-admin subuser modify --subuser=janedoe:swift --access=full
```

Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=management-modify-user-information](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=management-modify-user-information)*