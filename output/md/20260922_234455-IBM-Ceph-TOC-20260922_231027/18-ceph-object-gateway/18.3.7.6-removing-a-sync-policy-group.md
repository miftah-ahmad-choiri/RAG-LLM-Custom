# Removing a sync policy group

> **Index:** `18.3.7.6`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=policies-removing-sync-policy-group](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=policies-removing-sync-policy-group)

---

## Removing a sync policy group

Last Updated
: 2026-06-25

You can use the group remove command to remove the current sync policy
group by group ID, or to remove a specific bucket policy.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Root or `sudo` access.
- The Ceph Object Gateway is installed.

### Procedure

- Remove the current sync policy group or bucket policy. To remove a specific bucket policy, use
the `--bucket` option:

Syntax

```plaintext-ibm
radosgw-admin sync group remove --bucket=BUCKET_NAME --group-id=GROUP_ID
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# radosgw-admin sync group remove --group-id=mygroup
```

Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=policies-removing-sync-policy-group](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=policies-removing-sync-policy-group)*