# CephX key rotation

> **Index:** `10.3.4.7.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=rotation-cephx-key](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=rotation-cephx-key)

---

## CephX key rotation

Last Updated
: 2026-09-09

CephX key rotation updates authentication keys to use stronger cipher types after a cluster upgrade.

> 📝 **Note:** Important:
`aes256k`
key types are only supported on version
9.9.1.2
or later.

CephX is the authentication mechanism used by Ceph clients and daemons. Keys are associated with each entity and used for authentication.

After upgrading Ceph, existing keys might use older cipher types such as `aes`. Newer versions introduce stronger ciphers such as `aes256k`.

Service daemon keys for `mon`, `mgr`, `mds`, and `osd` are rotated automatically by `cephadm` during Ceph upgrade. Client CephX keys are not rotated automatically by `cephadm`.

Some services require additional actions after key rotation. For example, SMB andNFS services require redeployment after the SMB orNFS cluster keys and the SMB orNFS daemon Ceph Object Gateway keys are rotated so that the updated keyrings are applied.

During the transition, the cluster supports both old and new ciphers.After all entities that are reported by the
AUTH_INSECURE_CLIENT_KEY_TYPE
health warning are rotated, you can disable the old cipher.

To rotate CephX keys after upgrade, see Rotating CephX keys.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=rotation-cephx-key](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=rotation-cephx-key)*