# Reporting a peering failure

> **Index:** `14.7.4`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-reporting-peering-failure](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-reporting-peering-failure)

---

## Reporting a peering failure

Last Updated
: 2026-06-25

If a Ceph OSD daemon cannot peer with any of the Ceph OSD daemons defined in its Ceph
configuration file or the cluster map, it pings a Ceph Monitor for the most recent copy of the
cluster map every 30 seconds. The Ceph Monitor heartbeat interval can be changed.

You can change the Ceph Monitor heartbeat interval by setting the value at runtime:

Syntax

```plaintext-ibm
ceph config set osd osd_mon_heartbeat_interval TIME_IN_SECONDS
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config set osd osd_mon_heartbeat_interval 60
```

Copy to clipboard

Figure 1.
Report peering failure

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-reporting-peering-failure](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-reporting-peering-failure)*