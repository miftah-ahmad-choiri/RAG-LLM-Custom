# Disabling Cephx

> **Index:** `14.4.3`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-disabling-cephx](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-disabling-cephx)

---

## Disabling Cephx

Last Updated
: 2026-06-25

If your cluster environment is relatively safe, you can offset the computation expense of
the running authentication, by disabling Cephx.

The following procedure describes how to disable Cephx.

> 📝 **Note:** Important:
IBM
recommends
enabling authentication.

However, it may be easier during setup or troubleshooting to temporarily disable
authentication.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Root-level access to the Ceph Monitor node.

### Procedure

1. Disable `cephx` authentication by setting the following options in the
`[global]` section of the Ceph configuration file:

Example

```plaintext-ibm
    auth_cluster_required = none
    auth_service_required = none
    auth_client_required = none
```

Copy to clipboard
2. Start or restart the Ceph storage cluster.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-disabling-cephx](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-disabling-cephx)*