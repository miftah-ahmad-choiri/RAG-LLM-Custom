# Verifying firewall rules are configured for default Ceph ports

> **Index:** `14.2.6`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=cnc-verifying-firewall-rules-are-configured-default-ceph-ports](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=cnc-verifying-firewall-rules-are-configured-default-ceph-ports)

---

## Verifying firewall rules are configured for default Ceph ports

Last Updated
: 2026-06-25

Verify that the host’s firewall allows connection on the default TCP ports.

By default,
IBM Storage Ceph
daemons use TCP ports 6800—​7100 to communicate with other hosts in
the cluster.

> 📝 **Note:** Note:
If your network has a dedicated firewall, you might need to verify its
configuration in addition to following this procedure. See the firewall’s documentation for more
information.

See the firewall’s documentation for more information.

### Prerequisites

- Root-level access to the host.

### Procedure

1. Verify the host’s
`iptables`
configuration.

1. List active rules.
```plaintext-ibm
[root@host1 ~]# iptables -L
```

Copy to clipboard
2. Verify the absence of rules that restrict connectivity on TCP ports 6800—7100.
Example

```plaintext-ibm
REJECT all -- anywhere anywhere reject-with icmp-host-prohibited
```

Copy to clipboard

  1. List active rules.
  ```plaintext-ibm
  [root@host1 ~]# iptables -L
  ```



  Copy to clipboard
  2. Verify the absence of rules that restrict connectivity on TCP ports 6800—7100.
  Example

  ```plaintext-ibm
  REJECT all -- anywhere anywhere reject-with icmp-host-prohibited
  ```



  Copy to clipboard
2. Verify the host’s
`firewalld`
configuration.

1. List ports open on the host.
Syntax

```plaintext-ibm
firewall-cmd --zone ZONE --list-ports
```

Copy to clipboard

Example

```plaintext-ibm
[root@host1 ~]# firewall-cmd --zone default --list-ports
```

Copy to clipboard
2. Verify the range is inclusive of TCP ports 6800—7100.

  1. List ports open on the host.
  Syntax

  ```plaintext-ibm
  firewall-cmd --zone ZONE --list-ports
  ```



  Copy to clipboard

  Example

  ```plaintext-ibm
  [root@host1 ~]# firewall-cmd --zone default --list-ports
  ```



  Copy to clipboard
  2. Verify the range is inclusive of TCP ports 6800—7100.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=cnc-verifying-firewall-rules-are-configured-default-ceph-ports](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=cnc-verifying-firewall-rules-are-configured-default-ceph-ports)*