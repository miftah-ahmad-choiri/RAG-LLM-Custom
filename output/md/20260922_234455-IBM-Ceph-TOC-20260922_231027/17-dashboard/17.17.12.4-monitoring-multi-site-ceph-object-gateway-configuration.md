# Monitoring multi-site Ceph Object Gateway configuration

> **Index:** `17.17.12.4`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-monitoring-multi-site-ceph-object-gateway](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-monitoring-multi-site-ceph-object-gateway)

---

## Monitoring multi-site Ceph Object Gateway configuration

Last Updated
: 2026-06-25

The IBM Storage Ceph dashboard supports monitoring the
users and buckets of one zone in another zone in a multi-site Ceph Object Gateway configuration. For
example, if the users and buckets are created in a zone in the primary site, you can monitor those
users and buckets in the secondary zone in the secondary site.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- Minimum of one running IBM Storage Ceph cluster deployed on
both sites.
- Dashboard is installed.
- The multi-site Ceph Object Gateway is configured on the primary and secondary sites.
- Ceph Object Gateway login credentials of the primary and secondary sites are added to the
dashboard.
- Ceph Object Gateway users are created on the primary site.
- Ceph Object Gateway buckets are created on the primary site.

### Procedure

1. From the dashboard navigation of the secondary site, go to
Object
>
Buckets
.
2. View the Object Gateway buckets on the secondary landing page that were created for the
Object Gateway users on the primary site.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-monitoring-multi-site-ceph-object-gateway](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-monitoring-multi-site-ceph-object-gateway)*