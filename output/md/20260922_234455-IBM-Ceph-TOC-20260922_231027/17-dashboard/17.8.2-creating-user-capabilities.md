# Creating user capabilities

> **Index:** `17.8.2`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=capabilities-creating-user](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=capabilities-creating-user)

---

## Creating user capabilities

Last Updated
: 2026-06-25

Create role-based access users with different capabilities on the Ceph
dashboard.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster.
- Dashboard is installed.
- Admin-level access to the dashboard.

### About this task

For details on different user capabilities, see User capabilities and Access capabilities.

### Procedure

1. From the dashboard navigation, go to
Administration
>
Ceph
Users
.
2. Click
Create
.
3. Fill in the
Create User
form.
1. User Entity
: Enter as
TYPE
.
ID
2. Entity
: This can be
`mon`
,
`mgr`
,
`osd`
, or
`mds`
.
3. Entity Capabilities
: Enter the capabilities that you can to
provide to the user. For example,
`allow *`
and
`profile crash`
are
some of the capabilities that can be assigned to the client.
> 📝 **Note:** Note:
You can add more entities to the user, based on the requirement.

  1. User Entity
  : Enter as
  TYPE
  .
  ID
  2. Entity
  : This can be
  `mon`
  ,
  `mgr`
  ,
  `osd`
  , or
  `mds`
  .
  3. Entity Capabilities
  : Enter the capabilities that you can to
  provide to the user. For example,
  `allow *`
  and
  `profile crash`
  are
  some of the capabilities that can be assigned to the client.
  > 📝 **Note:** Note:
  You can add more entities to the user, based on the requirement.
4. Click
Create User
.A notification displays that the
user is created successfully.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=capabilities-creating-user](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=capabilities-creating-user)*