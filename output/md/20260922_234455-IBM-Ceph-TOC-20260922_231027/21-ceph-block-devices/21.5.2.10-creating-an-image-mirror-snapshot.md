# Creating an image mirror-snapshot

> **Index:** `21.5.2.10`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=mirroring-creating-image-mirror-snapshot](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=mirroring-creating-image-mirror-snapshot)

---

## Creating an image mirror-snapshot

Last Updated
: 2026-06-25

Learn to create an image mirror-snapshot by using the command-line.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A minimum of two running IBM Storage Ceph clusters.
- Root-level access to the Ceph client nodes for the IBM Storage Ceph clusters.
- A CephX user with administrator-level capabilities.
- Access to the IBM Storage Ceph cluster where a snapshot mirror
will be created.

### About this task

Create a mirror snapshot to replicate changes in a Ceph Block Device image when using
snapshot-based mirroring.

> 📝 **Note:** Important:
By default, a maximum of 5 image mirror-snapshots are retained. The most
recent image mirror-snapshot is automatically removed if the limit is reached. If required, the
limit can be overridden through the
`rbd_mirroring_max_mirroring_snapshots`
configuration. Image mirror-snapshots are automatically deleted when the image is removed or when
mirroring is disabled.

### Procedure

Create an image-mirror
snapshot.
Syntax

```plaintext-ibm
rbd --cluster CLUSTER_NAME mirror image snapshot POOL_NAME/IMAGE_NAME
```

Copy to clipboard

Example

```
[root@site-a ~]# rbd mirror image snapshot data/image1
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=mirroring-creating-image-mirror-snapshot](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=mirroring-creating-image-mirror-snapshot)*