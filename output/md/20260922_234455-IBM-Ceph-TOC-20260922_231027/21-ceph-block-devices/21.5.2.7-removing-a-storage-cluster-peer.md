# Removing a storage cluster peer

> **Index:** `21.5.2.7`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=mirroring-removing-storage-cluster-peer](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=mirroring-removing-storage-cluster-peer)

---

## Removing a storage cluster peer

Last Updated
: 2026-06-25

Learn to remove a storage cluster peer by using the command-line.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster.
- Root-level access to the node.

### About this task

Specify the peer UUID to remove a storage cluster peer.

### Procedure

Specify the pool name and the peer Universally Unique Identifier
(UUID).
Syntax:

```plaintext-ibm
rbd mirror pool peer remove POOL_NAME PEER_UUID
```

Copy to clipboard

```
[root@rbd-client ~]# rbd mirror pool peer remove data 7e90b4ce-e36d-4f07-8cbc-42050896825d
```

To view the peer UUID, use the
`rbd mirror pool info`
command.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=mirroring-removing-storage-cluster-peer](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=mirroring-removing-storage-cluster-peer)*