# Live migration formats

> **Index:** `21.2.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=images-live-migration-formats](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=images-live-migration-formats)

---

## Live migration formats

Last Updated
: 2026-09-09

`Native`, `qcow`, and `raw` are supported live migration format types and layouts.

Use the following `source-spec` JSON example file for live migration.

```plaintext-ibm
{
    "type": "native",
    ["cluster_name": "CLUSTER_NAME",] (specify if image in another cluster, requires `CLUSTER_NAME.conf` file)
    ["client_name": "CLIENT_NAME",] (for connecting to another cluster, default is `client.admin`)
    "pool_name": "POOL_NAME",
    ["pool_id": "POOL_ID",] (optional, alternative to "POOL_NAME" key)
    ["pool_namespace": "POOL_NAMESPACE",] (optional)
    "image_name": "IMAGE_NAME>",
    ["image_id": "IMAGE_ID",] (specify if image is in trash)
    "snap_name": "SNAP_NAME",
    ["snap_id": "SNAP_ID",] (optional, alternative to "SNAP_NAME" key)
}
```

Copy to clipboard

### native format

The
`native`
format does not include the stream object, since it utilizes native Ceph operations. For example, to import from the
rbd/ns1/image1@snap1
image, encode the
`source-spec`
as follows:

```plaintext-ibm
{
"type": "native",
"pool_name": "rbd",
"pool_namespace": "ns1",
"image_name": "image1",
"snap_name": "snap1"
}
```

Copy to clipboard

### qcow format

Use the `qcow` format to describe a QEMU copy-on-write (QCOW) block device. Both the QCOW v1 and v2 formats are currently supported with the exception of advanced features such as compression, encryption, backing files, and external data files.

Link the
`qcow`
format data to any supported stream source, by using the following
`source-spec`
file:

```plaintext-ibm
{
    "type": "qcow",
    "stream": {
      "type": "file",
  "file_path": "/mnt/image.qcow"
    }
}
```

Copy to clipboard

### raw format

Use the `raw` format to describe a thick-provisioned, raw block device export that is rbd export –export-format 1 SNAP_SPEC.

Link the
`raw`
format data to any supported stream source, by using the following
`source-spec`
file:

```plaintext-ibm
{
    "type": "raw",
    "stream": {
      "type": "file",
      "file_path": "/mnt/image-head.raw"
    },
    "snapshots": [
        {
            "type": "raw",
            "name": "snap1",
            "stream": {
              "type": "file",
       "file_path": "/mnt/image-snap1.raw"
            }
        },
    ] (optional oldest to newest ordering of snapshots)
}
```

Copy to clipboard

The inclusion of the `snapshots` array is optional and currently only supports thick-provisioned raw snapshot exports.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=images-live-migration-formats](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=images-live-migration-formats)*