# Disabling volumes plug-in

> **Index:** `20.12`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=systems-disabling-volumes-plug-in](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=systems-disabling-volumes-plug-in)

---

## Disabling volumes plug-in

Last Updated
: 2026-06-25

Learn how to disable volume plug-ins.

By default the volumes plug-in is enabled and set to always on. However, in certain cases, it
might be appropriate to disable it. For example, when a CephFS is in a degraded state, the volumes
plug-in commands can accumulate in MGR instead of getting served. This eventually causes policy
throttles to take effect and the MGR becomes unresponsive.

In this event, you can disable the volumes plug-in even though it is an always on module in
MGR.

Run the following
command:

```plaintext-ibm
ceph mgr module disable volumes --yes-i-really-mean-i
```

Copy to clipboard

> 📝 **Note:** Warning:
This command disables operations and removes commands of the volumes plug-in
as it disables all CephFS services on the Ceph cluster that is accessed through this plug-in.

Another way for disabling volume plug-ins is to disable asynchronous threads that are started by
volumes plug-ins for cloning and purging trash.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=systems-disabling-volumes-plug-in](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=systems-disabling-volumes-plug-in)*