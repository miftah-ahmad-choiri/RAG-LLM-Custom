# Listing a file system subvolume

> **Index:** `20.10.2.2`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-listing-file-system-subvolume](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-listing-file-system-subvolume)

---

## Listing a file system subvolume

Last Updated
: 2026-06-25

Learn how to list a Ceph File System (CephFS) subvolume.

### Before you begin

Be sure that you have a CephFS subvolume.

### Procedure

List the CephFS subvolume.

> 📝 **Note:** Note:
The
`--group_name`
parameter is optional. If the subvolume needs to be listed
from within a subvolume group, then
`--group_name`
needs to be passed in the
command.

```plaintext-ibm
ceph fs subvolume ls VOLUME_NAME[--group_name SUBVOLUME_GROUP_NAME]
```

Copy to clipboard

For
example,

```
[root@mon ~]# ceph fs subvolume ls cephfs01 --group_name subgroup0
[

{

"name": "sub0"

}

]
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-listing-file-system-subvolume](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-listing-file-system-subvolume)*