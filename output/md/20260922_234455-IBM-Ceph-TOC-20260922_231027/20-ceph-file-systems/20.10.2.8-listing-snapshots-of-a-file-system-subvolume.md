# Listing snapshots of a file system subvolume

> **Index:** `20.10.2.8`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-listing-snapshots-file-system-subvolume](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-listing-snapshots-file-system-subvolume)

---

## Listing snapshots of a file system subvolume

Last Updated
: 2026-06-25

Learn how to list the snapshots of a Ceph File system (CephFS) subvolume.

### Before you begin

Be sure that you have a CephFS subvolume and snapshots of the subvolume.

### Procedure

List the snapshots of a CephFS subvolume.

> 📝 **Note:** Note:
The
`--group_name`
parameter is optional. If the subvolume is present within
the subvolume group, then
`--group_name`
needs to be passed in the command.

```plaintext-ibm
ceph fs subvolume snapshot ls VOLUME_NAME SUBVOLUME_NAME [--group_name SUBVOLUME_GROUP_NAME]
```

Copy to clipboard

For
example:

```
[root@mon ~]# ceph fs subvolume snapshot ls cephfs01 sub0 --group_name subgroup0

[
{
"name": "snap0"
}
]
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-listing-snapshots-file-system-subvolume](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-listing-snapshots-file-system-subvolume)*