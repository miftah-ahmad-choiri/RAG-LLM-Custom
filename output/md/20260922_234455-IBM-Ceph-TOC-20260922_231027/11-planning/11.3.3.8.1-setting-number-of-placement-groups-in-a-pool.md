# Setting number of placement groups in a pool

> **Index:** `11.3.3.8.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=interface-setting-number-placement-groups-in-pool](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=interface-setting-number-placement-groups-in-pool)

---

## Setting number of placement groups in a pool

Last Updated
: 2026-06-25

To set the number of placement groups in a pool, you must specify the number of placement
groups at the time you create the pool.

Once you set placement groups for a pool, you can increase or decrease the number of placement
groups. For more information, see Creating pools.

To change the number of placement groups, use the following command:

Syntax

```plaintext-ibm
ceph osd pool set POOL_NAME pg_num PG_NUM
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph osd pool set pool1 pg_num 60
set pool 2 pg_num to 60
```

Copy to clipboard

Once you increase or decrease the number of placement groups, you must also adjust the number of
placement groups for placement (`pgp_num`) before your cluster rebalances. The
`pgp_num` should be equal to the `pg_num`. To increase the number of
placement groups for placement, run the following:

Syntax

```plaintext-ibm
ceph osd pool set POOL_NAME pgp_num PGP_NUM
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph osd pool set pool1 pgp_num 60
set pool 2 pgp_num to 60
```

Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=interface-setting-number-placement-groups-in-pool](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=interface-setting-number-placement-groups-in-pool)*