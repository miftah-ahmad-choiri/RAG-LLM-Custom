# CRUSH storage strategies examples

> **Index:** `11.3.2.10`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=overview-crush-storage-strategies-examples](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=overview-crush-storage-strategies-examples)

---

## CRUSH storage strategies examples

Last Updated
: 2026-06-25

Set the CRUSH rules with device classes.

### About this task

CRUSH can handle scenarios when you want to have most pools default to OSDs backed by large
hard disk drives or some pools mapped to OSDs are backed by fast solid-state drives (SSDs). Use
device classes to set rules for CRUSH to handle specific scenarios.

### Procedure

1. Add a class to each device.
```plaintext-ibm
ceph osd crush set-device-class CLASS OSD_ID [OSD_ID]
```

Copy to clipboard
For
example,

```
[ceph:root@host01 /]# ceph osd crush set-device-class hdd osd.0 osd.1 osd.4 osd.5
[ceph:root@host01 /]# ceph osd crush set-device-class ssd osd.2 osd.3 osd.6 osd.7
```
2. Create rules to use the devices.
```plaintext-ibm
ceph osd crush rule create-replicated RULENAME ROOT FAILURE_DOMAIN_TYPE DEVICE_CLASS
```

Copy to clipboard
For
example,

```
[ceph:root@host01 /]# ceph osd crush rule create-replicated cold default host hdd
[ceph:root@host01 /]# ceph osd crush rule create-replicated hot default host ssd
```
3. Set the pools to use the rules.
```plaintext-ibm
ceph osd pool set POOL_NAME crush_rule RULENAME
```

Copy to clipboard
For
example,

```
[ceph:root@host01 /]# ceph osd pool set cold crush_rule hdd
[ceph:root@host01 /]# ceph osd pool set hot crush_rule ssd
```
You do not need
to manually edit the CRUSH map because one hierarchy can serve multiple classes of
devices.For example,

```
device 0 osd.0 class hdd
device 1 osd.1 class hdd
device 2 osd.2 class ssd
device 3 osd.3 class ssd
device 4 osd.4 class hdd
device 5 osd.5 class hdd
device 6 osd.6 class ssd
device 7 osd.7 class ssd

  host ceph-osd-server-1 {
      id -1
      alg straw2
      hash 0
      item osd.0 weight 1.00
      item osd.1 weight 1.00
      item osd.2 weight 1.00
      item osd.3 weight 1.00
  }

  host ceph-osd-server-2 {
      id -2
      alg straw2
      hash 0
      item osd.4 weight 1.00
      item osd.5 weight 1.00
      item osd.6 weight 1.00
      item osd.7 weight 1.00
  }

  root default {
      id -3
      alg straw2
      hash 0
      item ceph-osd-server-1 weight 4.00
      item ceph-osd-server-2 weight 4.00
  }

  rule cold {
      ruleset 0
      type replicated
      min_size 2
      max_size 11
      step take default class hdd
      step chooseleaf firstn 0 type host
      step emit
  }

  rule hot {
      ruleset 1
      type replicated
      min_size 2
      max_size 11
      step take default class ssd
      step chooseleaf firstn 0 type host
      step emit
  }
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=overview-crush-storage-strategies-examples](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=overview-crush-storage-strategies-examples)*