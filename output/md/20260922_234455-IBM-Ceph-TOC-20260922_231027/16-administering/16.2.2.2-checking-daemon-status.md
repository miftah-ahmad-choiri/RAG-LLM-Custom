# Checking daemon status

> **Index:** `16.2.2.2`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=services-checking-daemon-status](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=services-checking-daemon-status)

---

## Checking daemon status

Last Updated
: 2026-06-25

A daemon is a `systemd` unit that is running and is part of the service.
The daemon status can be checked by using the ceph orch ps command.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster.
- Log into the `cephadm` shell.

### About this task

You can check the following status of the daemons of the storage cluster using the
`ceph
orch ps`
command. This command checks provides the following information:

- Print a list of all the daemons.
- Query the status of the target daemon.

### Procedure

1. Print a list of daemons.
```plaintext-ibm
ceph orch ps [--daemon-type DAEMON_TYPE] [--service_name SERVICE_NAME] [--daemon_id DAEMON_ID] [--format FORMAT] [--refresh]
```

Copy to clipboard
For example,

```
[ceph: root@host01 /]# ceph orch ps
```
2. Verify the status of the service instance.
```plaintext-ibm
ceph orch ls [--daemon-type DAEMON_TYPE] [--daemon_id DAEMON_ID] [--refresh]
```

Copy to clipboard
For
example,

```
[ceph: root@host01 /]# ceph orch ps --daemon_type osd --daemon_id 0
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=services-checking-daemon-status](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=services-checking-daemon-status)*