# Switching between built-in and custom profiles

> **Index:** `16.1.10.9`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=scheduler-switching-between-built-in-custom-profiles](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=scheduler-switching-between-built-in-custom-profiles)

---

## Switching between built-in and custom profiles

Last Updated
: 2026-06-25

Understand how to switch between the `built-in` and
`custom` profiles.

In some cases, you might want to switch to the `custom` profile if you want
complete control over all the mClock configuration options. However, it is recommended not to use
the `custom` profile unless you are an advanced user.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Root-level access to the Ceph Monitor host.

### Procedure

Switch from *built-in* profile to *custom* profile

1. Log into the `cephadm` shell:

Example

```plaintext-ibm
[root@host01 ~]# cephadm shell
```

Copy to clipboard
2. Switch to the *custom* profile:

Syntax

```plaintext-ibm
ceph config set osd.OSD_ID osd_mclock_profile custom
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config set osd.0 osd_mclock_profile custom
```

Copy to clipboard

> 📝 **Note:** Note:
For optimal performance the profile must be set on all Ceph OSDs by using the following command:

```plaintext-ibm
[ceph: root@host01 /]# ceph config set osd osd_mclock_profile custom
```

Copy to clipboard
3. Optional: After switching to the *custom* profile, modify the desired mClock configuration
options:

Syntax

```plaintext-ibm
ceph config set osd.OSD_ID MCLOCK_CONFIGURATION_OPTION VALUE
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config set osd.0 osd_mclock_scheduler_client_res 0.5
```

Copy to clipboard

This example changes the client reservation IOPS ratio for a specific OSD `osd.0`
to 0.5 (50%)

> 📝 **Note:** Important:
Change the reservations of other services, such as background recovery and
background best-effort accordingly to ensure that the sum of the reservations does not exceed the
maximum proportion (1.0) of the IOPS capacity of the OSD.

Switch from *custom* profile to *built-in* profile

1. Log into the cephadm shell:

Example

```plaintext-ibm
[root@host01 ~]# cephadm shell
```

Copy to clipboard
2. Set the desired *built-in* profile:

Syntax

```plaintext-ibm
ceph config set osd osd_mclock_profile MCLOCK_PROFILE
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config set osd osd_mclock_profile high_client_ops
```

Copy to clipboard

This example sets the *built-in* profile to `high_client_ops` on all Ceph
OSDs.
3. Determine the existing custom mClock configuration settings in the database:

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config dump
```

Copy to clipboard
4. Remove the custom mClock configuration settings determined earlier:

Syntax

```plaintext-ibm
ceph config rm osd MCLOCK_CONFIGURATION_OPTION
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config rm osd osd_mclock_scheduler_client_res
```

Copy to clipboard

This example removes the configuration option `osd_mclock_scheduler_client_res`
that was set on all Ceph OSDs.

After all existing custom mClock configuration settings are removed from the central
configuration database, the configuration settings related to `high_client_ops` are
applied.
5. Verify the settings on Ceph OSDs:

Syntax

```plaintext-ibm
ceph config show osd.OSD_ID
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config show osd.0
```

Copy to clipboard

### Reference

- For the list of the mClock configuration options that cannot be modified with *_built-in_*
profiles, see mClock profile types.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=scheduler-switching-between-built-in-custom-profiles](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=scheduler-switching-between-built-in-custom-profiles)*