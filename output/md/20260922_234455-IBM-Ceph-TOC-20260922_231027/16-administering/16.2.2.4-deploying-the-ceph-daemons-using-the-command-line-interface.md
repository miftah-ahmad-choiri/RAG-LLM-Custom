# Deploying the Ceph daemons using the command line interface

> **Index:** `16.2.2.4`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=services-deploying-ceph-daemons-using-command-line-interface](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=services-deploying-ceph-daemons-using-command-line-interface)

---

## Deploying the Ceph daemons using the command line interface

Last Updated
: 2026-06-25

Using the Ceph Orchestrator, you can deploy the daemons such as Ceph Manager, Ceph
Monitors, Ceph OSDs, monitoring stack, and others using the `ceph orch` command.
Placement specification is passed as `--placement` argument with the Orchestrator
commands.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Hosts are added to the storage cluster.

### Procedures

1. Log into the `cephadm` shell:

Example

```plaintext-ibm
[root@host01 ~]# cephadm shell
```

Copy to clipboard
2. Use one of the following methods to deploy the daemons on the hosts:

- **Method 1**: Specify the number of daemons and the host names:

Syntax

```plaintext-ibm
ceph orch apply SERVICE_NAME --placement="NUMBER_OF_DAEMONS HOST_NAME_1 HOST_NAME_2 HOST_NAME_3"
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch apply mon --placement="3 host01 host02 host03"
```

Copy to clipboard
- **Method 2**: Add the labels to the hosts and then deploy the daemons using the labels:

1. Add the labels to the hosts:

Syntax

```plaintext-ibm
ceph orch host label add HOSTNAME_1 LABEL
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch host label add host01 mon
```

Copy to clipboard
2. Deploy the daemons with labels:

Syntax

```plaintext-ibm
ceph orch apply DAEMON_NAME label:LABEL
```

Copy to clipboard

Example

```plaintext-ibm
ceph orch apply mon label:mon
```

Copy to clipboard

  1. Add the labels to the hosts:


  Syntax


  ```plaintext-ibm
  ceph orch host label add HOSTNAME_1 LABEL
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  [ceph: root@host01 /]# ceph orch host label add host01 mon
  ```



  Copy to clipboard
  2. Deploy the daemons with labels:


  Syntax


  ```plaintext-ibm
  ceph orch apply DAEMON_NAME label:LABEL
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  ceph orch apply mon label:mon
  ```



  Copy to clipboard
- **Method 3**: Add the labels to the hosts and deploy using the `--placement`
argument:

1. Add the labels to the hosts:

Syntax

```plaintext-ibm
ceph orch host label add HOSTNAME_1 LABEL
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch host label add host01 mon
```

Copy to clipboard
2. Deploy the daemons using the label placement specification:

Syntax

```plaintext-ibm
ceph orch apply DAEMON_NAME --placement="label:LABEL"
```

Copy to clipboard

Example

```plaintext-ibm
ceph orch apply mon --placement="label:mon"
```

Copy to clipboard

  1. Add the labels to the hosts:


  Syntax


  ```plaintext-ibm
  ceph orch host label add HOSTNAME_1 LABEL
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  [ceph: root@host01 /]# ceph orch host label add host01 mon
  ```



  Copy to clipboard
  2. Deploy the daemons using the label placement specification:


  Syntax


  ```plaintext-ibm
  ceph orch apply DAEMON_NAME --placement="label:LABEL"
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  ceph orch apply mon --placement="label:mon"
  ```



  Copy to clipboard

  - **Method 1**: Specify the number of daemons and the host names:


  Syntax


  ```plaintext-ibm
  ceph orch apply SERVICE_NAME --placement="NUMBER_OF_DAEMONS HOST_NAME_1 HOST_NAME_2 HOST_NAME_3"
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  [ceph: root@host01 /]# ceph orch apply mon --placement="3 host01 host02 host03"
  ```



  Copy to clipboard
  - **Method 2**: Add the labels to the hosts and then deploy the daemons using the labels:


  1. Add the labels to the hosts:


  Syntax


  ```plaintext-ibm
  ceph orch host label add HOSTNAME_1 LABEL
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  [ceph: root@host01 /]# ceph orch host label add host01 mon
  ```



  Copy to clipboard
  2. Deploy the daemons with labels:


  Syntax


  ```plaintext-ibm
  ceph orch apply DAEMON_NAME label:LABEL
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  ceph orch apply mon label:mon
  ```



  Copy to clipboard

    1. Add the labels to the hosts:


    Syntax


    ```plaintext-ibm
    ceph orch host label add HOSTNAME_1 LABEL
    ```



    Copy to clipboard


    Example


    ```plaintext-ibm
    [ceph: root@host01 /]# ceph orch host label add host01 mon
    ```



    Copy to clipboard
    2. Deploy the daemons with labels:


    Syntax


    ```plaintext-ibm
    ceph orch apply DAEMON_NAME label:LABEL
    ```



    Copy to clipboard


    Example


    ```plaintext-ibm
    ceph orch apply mon label:mon
    ```



    Copy to clipboard
  - **Method 3**: Add the labels to the hosts and deploy using the `--placement`
  argument:


  1. Add the labels to the hosts:


  Syntax


  ```plaintext-ibm
  ceph orch host label add HOSTNAME_1 LABEL
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  [ceph: root@host01 /]# ceph orch host label add host01 mon
  ```



  Copy to clipboard
  2. Deploy the daemons using the label placement specification:


  Syntax


  ```plaintext-ibm
  ceph orch apply DAEMON_NAME --placement="label:LABEL"
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  ceph orch apply mon --placement="label:mon"
  ```



  Copy to clipboard

    1. Add the labels to the hosts:


    Syntax


    ```plaintext-ibm
    ceph orch host label add HOSTNAME_1 LABEL
    ```



    Copy to clipboard


    Example


    ```plaintext-ibm
    [ceph: root@host01 /]# ceph orch host label add host01 mon
    ```



    Copy to clipboard
    2. Deploy the daemons using the label placement specification:


    Syntax


    ```plaintext-ibm
    ceph orch apply DAEMON_NAME --placement="label:LABEL"
    ```



    Copy to clipboard


    Example


    ```plaintext-ibm
    ceph orch apply mon --placement="label:mon"
    ```



    Copy to clipboard

### Verification

- List the service:

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch ls
```

Copy to clipboard
- List the hosts, daemons, and processes:

Syntax

```plaintext-ibm
ceph orch ps --daemon_type=DAEMON_NAME
ceph orch ps --service_name=SERVICE_NAME
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch ps --daemon_type=mon
[ceph: root@host01 /]# ceph orch ps --service_name=mon
```

Copy to clipboard

### Reference

- Adding hosts

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=services-deploying-ceph-daemons-using-command-line-interface](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=services-deploying-ceph-daemons-using-command-line-interface)*