# Monitoring HAProxy

> **Index:** `16.1.2.3.8`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=dashboard-monitoring-haproxy](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=dashboard-monitoring-haproxy)

---

## Monitoring HAProxy

Last Updated
: 2026-06-25

The Ceph Object Gateway allows you to assign many instances of the object gateway to a
single zone, so that you can scale out as load increases. Since each object gateway instance has its
own IP address, you can use HAProxy to balance the load across Ceph Object Gateway
servers.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster.
- Admin-level access on the storage dashboard.
- An existing Ceph Object Gateway service, without SSL. If you want SSL service, the certificate
should be configured on the ingress service, not the Ceph Object Gateway service.
- Ingress service deployed using the Ceph Orchestrator.
- Monitoring stack components are created on the dashboard.

### About this task

You can monitor the following HAProxy metrics:

- Total responses by HTTP code.
- Total requests/responses.
- Total number of connections.
- Current total number of incoming / outgoing bytes.

You can also get the Grafana details by running the ceph dashboard
get-grafana-api-url command.

### Procedure

1. Log in to the Grafana URL and select
RGW Overview
panel.
```plaintext-ibm
https://DASHBOARD_URL:3000
```

Copy to clipboard
For example,

```
https://dashboard_url:3000
```
2. Verify the HAProxy metrics on the Grafana URL.
3. From the Ceph dashboard navigation, go to
Object
>
Gateways
.
4. From the
Overall Performance
tab, verify the Ceph Object Gateway
HAProxy metrics.Figure 1.
Viewing the Ceph Object Gateway HAProxy metrics

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=dashboard-monitoring-haproxy](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=dashboard-monitoring-haproxy)*