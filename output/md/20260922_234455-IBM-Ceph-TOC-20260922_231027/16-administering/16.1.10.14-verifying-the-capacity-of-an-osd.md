# Verifying the capacity of an OSD

> **Index:** `16.1.10.14`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=scheduler-verifying-capacity-osd](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=scheduler-verifying-capacity-osd)

---

## Verifying the capacity of an OSD

Last Updated
: 2026-06-25

You can verify the capacity of a Ceph OSD after setting up the storage
cluster.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Root-level access to the Ceph Monitor host.

### Procedure

1. Log into the `cephadm` shell:

Example

```plaintext-ibm
[root@host01 ~]# cephadm shell
```

Copy to clipboard
2. Verify the capacity of a Ceph OSD:

Syntax

```plaintext-ibm
ceph config show osd.OSD_ID osd_mclock_max_capacity_iops_[hdd, ssd]
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config show osd.0 osd_mclock_max_capacity_iops_ssd

21500.000000
```

Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=scheduler-verifying-capacity-osd](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=scheduler-verifying-capacity-osd)*