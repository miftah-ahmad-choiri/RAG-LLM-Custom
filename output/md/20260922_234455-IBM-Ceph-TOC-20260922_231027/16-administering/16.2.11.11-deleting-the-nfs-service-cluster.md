# Deleting the NFS service cluster

> **Index:** `16.2.11.11`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=orchestrator-deleting-nfs-service-cluster](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=orchestrator-deleting-nfs-service-cluster)

---

## Deleting the NFS service cluster

Last Updated
: 2026-06-25

You can use the Ceph Orchestrator with `cephadm` in the backend to delete the NFS service
cluster.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Hosts are added to the cluster.
- All manager, monitor and OSD daemons are deployed.
- NFS service cluster created using the `mgr/nfs` module.

### Procedure

1. Log into the `cephadm` shell:

Example

```plaintext-ibm
[root@host01 ~]# cephadm shell
```

Copy to clipboard
2. Delete the cluster:

Syntax

```plaintext-ibm
ceph nfs cluster delete CLUSTER_NAME
```

Copy to clipboard

The *CLUSTER_NAME* is an arbitrary string.

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph nfs cluster delete nfsganesha
NFS Cluster Deleted Successfully
```

Copy to clipboard

> 📝 **Note:** Note:
The
`delete`
option is deprecated and you need to use
`rm`
to
delete an NFS cluster.

### Verification

- List the cluster details:

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph nfs cluster ls
```

Copy to clipboard

### Reference

- Creating the NFS service cluster

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=orchestrator-deleting-nfs-service-cluster](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=orchestrator-deleting-nfs-service-cluster)*