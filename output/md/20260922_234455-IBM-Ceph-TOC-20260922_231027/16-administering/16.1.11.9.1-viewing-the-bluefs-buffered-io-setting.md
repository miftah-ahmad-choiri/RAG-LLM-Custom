# Viewing the bluefs_buffered_io setting

> **Index:** `16.1.11.9.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=bluefs-viewing-buffered-io-setting](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=bluefs-viewing-buffered-io-setting)

---

## Viewing the bluefs_buffered_io setting

Last Updated
: 2026-06-25

As a storage administrator, you can view the current setting for the bluefs_buffered_io parameter.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster.
- Root-level access to the Ceph Monitor node.
- Log into the `cephadm` shell, using the cephadm shell command.

### About this task

The option
bluefs_buffered_io
is set to
`True`
by default for
IBM Storage Ceph
. This option enable BlueFS to perform buffered reads in some cases, and enables the kernel page cache to act as a secondary cache for reads like RocksDB block reads.

> 📝 **Note:** Important:
Changing the value of
bluefs_buffered_io
is not recommended. Before changing the
bluefs_buffered_io
parameter, contact your
[IBM Support](https://www.ibm.com/mysupport/)
account team.

### Procedure

View the current value of the
bluefs_buffered_io
parameter, using one of the following procedures.

- View the value stored in the configuration database.

```plaintext-ibm
ceph config get osd bluefs_buffered_io
```

Copy to clipboard

For example,

```
[ceph: root@host01 /]# ceph config get osd bluefs_buffered_io
```
- View the value stored in the configuration database for a specific OSD.

```plaintext-ibm
ceph config get OSD_ID bluefs_buffered_io
```

Copy to clipboard

For example,

```
[ceph: root@host01 /]# ceph config get osd.2 bluefs_buffered_io
```
- View the running value for an OSD where the running value is different from the value stored in the configuration database.

```plaintext-ibm
ceph config show OSD_ID bluefs_buffered_io
```

Copy to clipboard

For example,

```
[ceph: root@host01 /]# ceph config show osd.3 bluefs_buffered_io
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=bluefs-viewing-buffered-io-setting](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=bluefs-viewing-buffered-io-setting)*