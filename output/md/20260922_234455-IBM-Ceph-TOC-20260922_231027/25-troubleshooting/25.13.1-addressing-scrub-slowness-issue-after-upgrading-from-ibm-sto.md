# Addressing scrub slowness issue after upgrading from IBM Storage Ceph 6 to IBM Storage Ceph 8 and IBM Storage Ceph 7 to IBM Storage Ceph 8

> **Index:** `25.13.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=tsdsi-addressing-scrub-slowness-issue-after-upgrading-from-storage-ceph-6-storage-ceph-8-storage-ceph-7-storage-ceph-8](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=tsdsi-addressing-scrub-slowness-issue-after-upgrading-from-storage-ceph-6-storage-ceph-8-storage-ceph-7-storage-ceph-8)

---

## Addressing scrub slowness issue after upgrading from IBM Storage Ceph 6 to IBM Storage Ceph 8 and IBM Storage Ceph 7 to IBM Storage Ceph 8

Last Updated
: 2026-06-25

Learn to troubleshoot the scrub slowness issue which seen after upgrading from IBM Storage Ceph 6 to IBM Storage Ceph 8 and IBM Storage Ceph 7 to IBM Storage Ceph 8.

### Before you begin

- A running IBM Storage Ceph cluster in a healthy state.
- Root-level access to the node.

### About this task

Scrub slowness is caused by the automated OSD benchmark setting a very low value for
osd_mclock_max_capacity_iops_hdd. Due to this, scrub operations are impacted
since the IOPS capacity of an OSD plays a significant role in determining the bandwidth the scrub
operation receives. To further increase the problem, scrubs receive only a fraction of the total
IOPS capacity based on the QoS allocation defined by the **mClock** profile.

Due to this, the Ceph cluster reports the expected scrub completion time in multiples of days or
weeks.

### Procedure

Detect low measured IOPS reported by OSD bench during OSD boot-up and fallback to default
IOPS setting defined for
osd_mclock_max_capacity_iops_[hdd|ssd]
. The fallback
is triggered if the reported IOPS falls below a threshold determined by
osd_mclock_iops_capacity_low_threshold_[hdd|ssd]
. A cluster warning is also
logged.
Example:

```plaintext-ibm
$ ceph config rm osd.X osd_mclock_max_capacity_iops_[hdd|ssd]
```

Copy to clipboard

- Perform the following steps if you have not yet upgraded to IBM Storage Ceph 7 from IBM Storage Ceph 6 (before the upgrade):

1. For clusters already affected by the issue, remove the IOPS capacity setting on the
OSD(s) before upgrading to the release with the fix by running the following
command:For
example:

```plaintext-ibm
$ ceph config rm osd.X osd_mclock_max_capacity_iops_[hdd|ssd]
```

Copy to clipboard
2. Set the
osd_mclock_force_run_benchmark_on_init
option for the
affected OSD to
true
before the upgrade:For
example:

```plaintext-ibm
$ ceph config set osd.X osd_mclock_force_run_benchmark_on_init true
```

Copy to clipboard

After
upgrading to the release with this fix, the IOPS capacity reflects the default setting or the new
one reported by the OSD bench.

- Perform the following steps:

1. If you were unable to perform the above steps before upgrade, you re-run the OSD bench
again after upgrading by removing the
osd_mclock_max_capacity_iops_[hdd|ssd]
setting:Example:

```plaintext-ibm
$ ceph config rm osd.X osd_mclock_max_capacity_iops_[hdd|ssd]
```

Copy to clipboard
2. Set
osd_mclock_force_run_benchmark_on_init
to
true
.Example:

```plaintext-ibm
$ ceph config set osd.X osd_mclock_force_run_benchmark_on_init true
```

Copy to clipboard
3. Restart the OSD.
After the OSD restarts, the IOPS capacity reflects the default setting or the new setting
reported by the OSD bench.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=tsdsi-addressing-scrub-slowness-issue-after-upgrading-from-storage-ceph-6-storage-ceph-8-storage-ceph-7-storage-ceph-8](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=tsdsi-addressing-scrub-slowness-issue-after-upgrading-from-storage-ceph-6-storage-ceph-8-storage-ceph-7-storage-ceph-8)*