# Listing object map

> **Index:** `25.9.2.3`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-listing-object-map](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-listing-object-map)

---

## Listing object map

Last Updated
: 2026-06-25

Use the `ceph-objectstore-tool` utility to list the contents of the object
map (OMAP).

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- Root-level access to the Ceph OSD node.
- Stopping the `ceph-osd` daemon.

### About this task

Listing the contents of the OMAP provides a list of keys.

### Procedure

1. Verify that the appropriate OSD is down.
```plaintext-ibm
systemctl status ceph-osd@OSD_ID
```

Copy to clipboard
For
example,

```
[root@host01 ~]# systemctl status ceph-osd@1
```
2. Log in to the OSD container.
```plaintext-ibm
cephadm shell --name osd.OSD_ID
```

Copy to clipboard
For
example,

```
[root@host01 ~]# cephadm shell --name osd.0
```
3. List the object map.
```plaintext-ibm
ceph-objectstore-tool --data-path PATH_TO_OSD --pgid PG_ID OBJECT list-omap
```

Copy to clipboard
For
example,

```
[ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 --pgid 0.1c \
'{"oid":"zone_info.default","key":"","snapid":-2,"hash":235010478,"max":0,"pool":11,"namespace":""}' \
list-omap
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-listing-object-map](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-listing-object-map)*