# Listing placement groups stuck in stale, inactive, or unclean states

> **Index:** `25.8.2`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=tcpg-listing-placement-groups-stuck-in-stale-inactive-unclean-states](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=tcpg-listing-placement-groups-stuck-in-stale-inactive-unclean-states)

---

## Listing placement groups stuck in stale, inactive, or unclean states

Last Updated
: 2026-06-25

After a failure, placement groups enter states such as degraded or
peering. These states indicate normal progression through the failure recovery process.
If a placement group stays in one of these states for a longer time than expected, it can be an
indication of a larger problem.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster.
- Root-level access to the node.

### About this task

The Monitors report when placement groups (PGs) get stuck in a state that is not optimal.

The `mon_pg_stuck_threshold` option in the Ceph configuration file determines the
number of seconds after which placement groups are considered inactive,
unclean, or stale.

Table 1 lists
the `mon_pg_stuck_threshold` states along with their descriptions.

| State | What it means | Most common causes | See |
| --- | --- | --- | --- |
| inactive | The PG is not able to service read/write requests. | Peering problems | Inactive placement groups |
| unclean | The PG contains objects that are not replicated the desired number of times. Something is preventing the PG from recovering. | Unfound objects OSDs are down Incorrect configuration | Unclean placement groups |
| stale | The status of the PG has not been updated by a ceph-osd daemon. | OSDs are down | Stale placement groups |

### Procedure

1. Log in to the
`cephadm`
shell.For
example,

```
[root@host01 ~]# cephadm shell
```
2. List the stuck PGs.For
example,

```
[ceph: root@host01 /]# ceph pg dump_stuck inactive
[ceph: root@host01 /]# ceph pg dump_stuck unclean
[ceph: root@host01 /]# ceph pg dump_stuck stale
```
For for more information,
see
Placement Group states
.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=tcpg-listing-placement-groups-stuck-in-stale-inactive-unclean-states](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=tcpg-listing-placement-groups-stuck-in-stale-inactive-unclean-states)*