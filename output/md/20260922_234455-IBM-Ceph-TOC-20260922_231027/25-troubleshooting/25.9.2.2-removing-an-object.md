# Removing an object

> **Index:** `25.9.2.2`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-removing-object](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-removing-object)

---

## Removing an object

Last Updated
: 2026-06-25

Use the `ceph-objectstore-tool` utility to remove an object. By removing
an object, its contents and references are removed from the placement group (PG).

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- Root-level access to the Ceph OSD node.
- Stopping the `ceph-osd` daemon.

### About this task

> 📝 **Note:** Important:
You cannot recreate an object after it is removed.

### Procedure

1. Log in to the OSD container.
```plaintext-ibm
cephadm shell --name osd.OSD_ID
```

Copy to clipboard
For
example,

```
[root@host01 ~]# cephadm shell --name osd.0
```
2. Remove an object.
```plaintext-ibm
ceph-objectstore-tool --data-path PATH_TO_OSD --pgid PG_ID \
OBJECT \
remove
```

Copy to clipboard
For
example,

```
[ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 --pgid 0.1c \
'{"oid":"zone_info.default","key":"","snapid":-2,"hash":235010478,"max":0,"pool":11,"namespace":""}' \
remove
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-removing-object](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-removing-object)*