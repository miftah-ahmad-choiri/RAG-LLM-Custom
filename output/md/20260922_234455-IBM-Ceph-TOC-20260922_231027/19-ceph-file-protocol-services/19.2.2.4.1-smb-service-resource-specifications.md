# SMB service resource specifications

> **Index:** `19.2.2.4.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=method-smb-service-resource-specifications](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=method-smb-service-resource-specifications)

---

## SMB service resource specifications

Last Updated
: 2026-06-25

Learn about the different resource types and their attributes and values.

The resource YAML block contains a list of model-defined resource types, attributes, and values for the application. Each resource is added as a new line and key: value mappings define the values of the attributes that are associated with the resource type.

Following are the resource specification types:

- Cluster resources
- Share resource
- Join-auth resource
- Users-and-group resource

### Cluster resources

The following list explains the various parameters available in the `cluster resources` specification type.

resource_type

A literal string `ceph.smb.cluster`.

cluster_id

A short string identifying the cluster.

auth_mode

One of `user` or `active-directory`

intent

One of `present` or `removed`. If not provided, `present` is assumed.

domain_settings

Object. Ignored/optional for `user` auth. Required for `active-directory` Fields:

realm

Required string. AD domain/realm name.

join_sources

Required list. Each element is an object.

user_group_settings

List. Ignored/optional for `active-directory`. Each element is an object.

custom_dns

Optional. List of IP addresses. IP addresses are used as DNS resolvers in SMB containers allowing the containers to use domain DNS even if the Ceph host does not.

placement

Optional. A Ceph Orchestration placement specifier. Defaults to one host if not provided

clustering

Optional. Control if a cluster abstraction actually uses SMB's clustering mechanism. The value may be one of `default`, `always`, or `never`. A `default` value indicates that clustering should be enabled if the placement count value is any value other than 1. A value of `always` enables clustering regardless of the placement count. A value of `never` disables clustering regardless of the placement count. If unspecified, `default` is assumed.

public_addrs

List of objects; optional. Supported only when using SMB's clustering. Assign virtual IP addresses that will be managed by the clustering subsystem and may automatically move between nodes running SMB containers. Available public_addrs fields are:

address

Required string. An IP address with a required prefix length (example: `192.168.4.51/24`). This address is assigned to one of the host's network devices and managed automatically.

destination

Optional. String or a list of strings. A `destination` defines where the system will assign the managed IPs. Each string value must be a network address (example `192.168.4.0/24`). One or more destinations may be supplied. The typical case is to use exactly one destination and so the value may be supplied as a string, rather than a list with a single item. Each destination network is mapped to a device on a host. Run `cephadm list-networks` for an example of these mappings. If destination is not supplied the network is automatically determined that uses the address value that is supplied and taken as the destination.

custom_smb_global_options

Optional mapping. Specify key-value pairs that will be directly added to the global `smb.conf` options (or equivalent) of an SMB server. Do *not* use this option unless you are prepared to debug the SMB instances yourself.

This option is meant for developers, feature investigators, and other advanced users to take more direct control of a share's options without needing to change the Ceph codebase. Entries in this map should match parameters in `smb.conf` and their values. A special key `_allow_customization` must appear somewhere in the mapping with the value of `i-take-responsibility-for-all-samba-configuration-errors` as an indicator that the user is aware that using this option can easily break things in ways that the Ceph team cannot help with. This special key will automatically be removed from the list of options that are passed to SMB.

> 📝 **Note:** Warning:

The `clustering` option lets you control when SMB's CTDB clustering is used. By default, clustering is based on the placement count. If you choose to manually set `clustering`, make sure you understand how it interacts with placement.

Running multiple instances of `smb` without enabling the clustering option leads to unexpected issues.

A join source object supports the following fields:

source_type

Optional. Must be `resource` if specified.

ref

String. Required for `source_type: resource`. Must refer to the ID of a `ceph.smb.join.auth` resource

A user group source object supports the following fields:

source_type

Optional. One of `resource` (the default) or `empty`

ref

String. Required for `source_type: resource`. Must refer to the ID of a `ceph.smb.join.auth` resource

Examples:

- The following is an example of a cluster that is configured for AD membership:

```
resource_type: ceph.smb.cluster
cluster_id: tango
auth_mode: active-directory
domain_settings:
  realm: DOMAIN1.SINK.TEST
  join_sources:
    # this join source refers to a join auth resource with id "join1-admin"
    - source_type: resource
      ref: join1-admin
custom_dns:
  - "192.168.76.204"
placement:
  count: 1
```
- The following is an example of a cluster that is configured for standalone operation:

```
resource_type: ceph.smb.cluster
cluster_id: rhumba
auth_mode: user
user_group_settings:
  - source_type: resource
    ref: ug1
placement:
  hosts:
    - node6.mycluster.sink.test
```
- An example cluster resource with intent to remove:

```
resource_type: ceph.smb.cluster
cluster_id: rhumba
intent: removed
```

### Share resource

The following list explains the various available parameters in the `share resource` specification type:

resource_type

A literal string `ceph.smb.share`.

cluster_id

A short string identifying the cluster.

share_id

A short string identifying the share. Must be Unique within a cluster.

intent

One of `present` or `removed`. If not provided, `present` is assumed.

name

Optional string. The display name presented to SMB clients. This parameter is independent of intent and corresponds to the `--share-name` flag in imperative commands.

If not specified, the share_id value is used as the share name.

The share name must follow these validation rules:

- Maximum length: 64 characters
- Must start with alphanumeric (a-z, A-Z, 0-9) or underscore (_)
- May contain alphanumeric characters, spaces, underscores (_), periods (.), or hyphens (-)
- Pattern: `^[a-zA-Z0-9_][a-zA-Z0-9. _-]{,63}$`

readonly

Optional Boolean, defaulting to false. If true no clients are permitted to write to the share.

browseable

Optional Boolean, defaulting to true. If true the share will be included in share listings visible to clients.

cephfs

Required object. Fields:

volume

Required string. Name of the Cephfs volume to use.

path

Required string. Path within the volume or subvolume to share.

subvolumegroup

Optional string. Name of a subvolumegroup to share.

subvolume

Optional string. Name of a subvolume to share. If `subvolumegroup` is not set and this value contains exactly one forward slash (`/`) character, the value will automatically be split into `subvolumegroup` and `subvolume` parts.

provider

Optional. One of `samba-vfs` or `kcephfs` (`kcephfs` is not yet supported). Selects how CephFS storage must be provided to the share.

restrict_access

Optional Boolean, defaulting to false. If true the share will only permit access by users who are explicitly listed in `login_control`.

login_control

Optional list of objects. Fields:

name

Required string. Name of the user or group.

category

Optional. One of `user` (default) or `group`.

access

One of `read` (alias `r`), `read-write` (alias `rw`), `none`, or `admin`. Specific access level to grant to the user or group when logging in to this share. The `none` value denies access to the share regardless of the `restrict_access` value.

custom_smb_share_options

Optional mapping. Specify key-value pairs that will be directly added to the `smb.conf` (or equivalent) of an SMB server. Do *not* use this option unless you are prepared to debug the SMB instances yourself.

This option is meant for developers, feature investigators, and other advanced users to take more direct control of a share's options without needing to change the Ceph codebase. Entries in this map should match parameters in `smb.conf` and their values. A special key `_allow_customization` must appear somewhere in the mapping with the value of `i-take-responsibility-for-all-samba-configuration-errors` as an indicator that the user is aware that using this option can easily break things in ways that the Ceph team cannot help with. This special key will automatically be removed from the list of options that are passed to SMB service.

Examples:

- The following is an example of a share:

```
resource_type: ceph.smb.share
cluster_id: tango
share_id: sp1
name: "Staff Pics"
cephfs:
  volume: cephfs
  path: /pics
  subvolumegroup: smbshares
  subvolume: staff
```
- The following is an example of a share with an intent to be removed:

```
resource_type: ceph.smb.share
cluster_id: tango
share_id: sp2
intent: removed
```

### Join-auth resource

The following list explains the various parameters available for the `join-auth` resource specification type:

resource_type

A literal string `ceph.smb.join.auth`.

auth_id

A short string identifying the join auth resource.

intent

One of `present` or `removed`. If not provided, `present` is assumed.

auth

Required object. Fields:

username

Required string. User with ability to join a system to Active Directory (AD).

password

Required string. The AD user's password.

linked_to_cluster:

Optional. A string containing a cluster ID. If set, the resource may only be used with the linked cluster and will automatically be removed when the linked cluster is removed.

For example:

```
resource_type: ceph.smb.join.auth
auth_id: join1-admin
auth:
  username: Administrator
  password: Passw0rd
```

### Users-and-group resource

The following list explains the various available parameters in the `users-and-group` resource specification type.

resource_type

A literal string `ceph.smb.usersgroups`

users_groups_id

A short string identifying the users and groups resource

intent

One of `present` or `removed`. If not provided, `present` is assumed.

values

Required object. Fields:

users

List of objects. Fields:

name

A username.

password
A password.
groups

List of objects. Fields:

name

The name of the group.

linked_to_cluster:

Optional. A string containing a cluster ID. If set, the resource may only be used with the linked cluster and will automatically be removed when the linked cluster is removed.

For example:

```
resource_type: ceph.smb.usersgroups
users_groups_id: ug1
values:
  users:
    - name: chuckx
      password: 3xample101
    - name: steves
      password: F00Bar123
    groups: []
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=method-smb-service-resource-specifications](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=method-smb-service-resource-specifications)*