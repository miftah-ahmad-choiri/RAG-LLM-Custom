# Managing buckets of a multi-site object configuration

> **Index:** `17.17.12`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=gateway-managing-buckets-multi-site-object-configuration](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=gateway-managing-buckets-multi-site-object-configuration)

---

## Managing buckets of a multi-site object configuration

Last Updated
: 2026-06-25

As a storage administrator, you can edit buckets of one zone in another zone on the IBM Storage Ceph Dashboard. However, you can delete buckets of
secondary sites in the primary site. You cannot delete the buckets of master zones of primary sites
in other sites. For example, If the buckets are created in a zone in the secondary site, you can
edit and delete those buckets in the master zone in the primary site.

For more information about multi-site and Ceph Object Gateway buckets, see:

- Multi-site configuration and administration
- Manually adding Ceph Object Gateway login credentials to the dashboard
- Managing Ceph Object Gateway buckets
- User roles and permissions

### Prerequisites

- At least one running IBM Storage Ceph cluster deployed on both
the sites.
- Dashboard is installed.
- The multi-site Ceph Object Gateway is configured on the primary and secondary sites.
- Ceph Object Gateway login credentials of the primary and secondary sites are added to the
dashboard.
- Ceph Object Gateway users are created on the primary site.
- Ceph Object Gateway buckets are created on the primary site.
- At least `rgw-manager` level of access on the Ceph dashboard.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=gateway-managing-buckets-multi-site-object-configuration](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=gateway-managing-buckets-multi-site-object-configuration)*