# Enabling single sign-on with SAML 2.0

> **Index:** `17.5.3`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=dashboard-enabling-single-sign-saml-20](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=dashboard-enabling-single-sign-saml-20)

---

## Enabling single sign-on with SAML 2.0

Last Updated
: 2026-06-25

Enable single sign-on (SSO) for the Ceph Dashboard with SAML 2.0.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster.
- Installation of the Ceph Dashboard.
- Root-level access to the Ceph Manager hosts.

### Procedure

1. Configure SAML 2.0 SSO on the Ceph Dashboard.
```plaintext-ibm
ceph dashboard sso setup saml2 CEPH_DASHBOARD_BASE_URL IDP_METADATA IDP_USERNAME_ATTRIBUTE IDP_ENTITY_ID SP_X_509_CERT SP_PRIVATE_KEY
```

Copy to clipboard

Replace the following variables:

- CEPH_DASHBOARD_BASE_URL with the base URL where Ceph Dashboard is
accessible.
- IDP_METADATA with the URL to remote or local path or content of the IdP
metadata XML. The supported URL types are http, https,
and file.
- (Optional) IDP_USERNAME_ATTRIBUTE with the attribute used to get the username
from the authentication response. Defaults to `uid`.
- (Optional) IDP_ENTITY_ID with the IdP entity ID when more than one entity ID
exists on the IdP metadata.
- (Optional) SP_X_509_CERT with the file path of the certificate used by Ceph
Dashboard for signing and encryption.
- (Optional) SP_PRIVATE_KEY with the file path of the private key used by Ceph
Dashboard for signing and encryption.
For
example,

```
[ceph: root@host01 /]# ceph dashboard ceph dashboard sso setup saml2 https://dashboard_hostname.ceph.redhat.com:8443 idp-metadata.xml username https://10.70.59.125:8080/auth/realms/realm_name /home/certificate.txt /home/private-key.txt
```

  - CEPH_DASHBOARD_BASE_URL with the base URL where Ceph Dashboard is
  accessible.
  - IDP_METADATA with the URL to remote or local path or content of the IdP
  metadata XML. The supported URL types are http, https,
  and file.
  - (Optional) IDP_USERNAME_ATTRIBUTE with the attribute used to get the username
  from the authentication response. Defaults to `uid`.
  - (Optional) IDP_ENTITY_ID with the IdP entity ID when more than one entity ID
  exists on the IdP metadata.
  - (Optional) SP_X_509_CERT with the file path of the certificate used by Ceph
  Dashboard for signing and encryption.
  - (Optional) SP_PRIVATE_KEY with the file path of the private key used by Ceph
  Dashboard for signing and encryption.
2. Verify the SAML 2.0 configuration.
```plaintext-ibm
cephadm shell ceph dashboard sso show saml2
```

Copy to clipboard
For
example,

```
[ceph: root@host01 /]# ceph dashboard ceph dashboard sso show saml2
```
3. Enable SSO.
```plaintext-ibm
cephadm shell ceph dashboard sso enable saml2
```

Copy to clipboard
For
example,

```
[ceph: root@host01 /]# ceph dashboard ceph dashboard sso enable saml2
SSO is "enabled" with "SAML2" protocol.
```

### What to do next

1. Open your dashboard
URL.
https://dashboard_hostname.ceph.redhat.com:8443
2. On the SSO page, enter the login credentials.
The SSO redirects to the dashboard web
interface.

> 📝 **Note:** Note:
Check the SSO status at any time with the
cephadm shell ceph dashboard sso
status
command. For
example:

```
[ceph: root@host01 /]# ceph dashboard ceph dashboard sso status
SSO is "enabled" with "SAML2" protocol.
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=dashboard-enabling-single-sign-saml-20](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=dashboard-enabling-single-sign-saml-20)*