# Erasure coding with overwrites

> **Index:** `11.3.5.3`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=overview-erasure-coding-overwrites](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=overview-erasure-coding-overwrites)

---

## Erasure coding with overwrites

Last Updated
: 2026-06-25

By default, erasure coded pools only work with the Ceph Object Gateway, which performs
full object writes and appends.

Using erasure coded pools with overwrites allows Ceph Block Devices and CephFS store their data
in an erasure coded pool:

Syntax

```plaintext-ibm
ceph osd pool set ERASURE_CODED_POOL_NAME allow_ec_overwrites true
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph osd pool set ec_pool allow_ec_overwrites true
```

Copy to clipboard

Enabling erasure coded pools with overwrites can only reside in a pool using BlueStore OSDs.
Since BlueStore’s checksumming is used to detect bit rot or other corruption during deep scrubs.
Using FileStore with erasure coded overwrites is unsafe, and yields lower performance when compared
to BlueStore.

Erasure coded pools do not support omap. To use erasure coded pools with Ceph Block Devices and
CephFS, store the data in an erasure coded pool, and the metadata in a replicated pool.

For Ceph Block Devices, use the `--data-pool` option during image creation:

Syntax

```plaintext-ibm
rbd create --size IMAGE_SIZE_M|G|T --data-pool _ERASURE_CODED_POOL_NAME REPLICATED_POOL_NAME/IMAGE_NAME
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# rbd create --size 1G --data-pool ec_pool rep_pool/image01
```

Copy to clipboard

If using erasure coded pools for CephFS, then setting the overwrites must be done in a file
layout.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=overview-erasure-coding-overwrites](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=overview-erasure-coding-overwrites)*