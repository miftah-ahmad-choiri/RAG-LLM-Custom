# Setting an OSD’s weight by placement group distribution

> **Index:** `11.3.2.5.5`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=weights-setting-osds-weight-by-placement-group-distribution](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=weights-setting-osds-weight-by-placement-group-distribution)

---

## Setting an OSD’s weight by placement group distribution

Last Updated
: 2026-06-25

In CRUSH hierarchies with a smaller number of OSDs, it’s possible for some OSDs to get
more placement groups (PGs) than other OSDs, resulting in a higher load. You can reweight OSDs by PG
distribution to address this situation.

Reweight OSDs by PG distribution, by running the following:

Syntax

```plaintext-ibm
osd reweight-by-pg POOL_NAME
```

Copy to clipboard

Where:

- `poolname` is the name of the pool. Ceph will examine how the pool assigns PGs to
OSDs and reweight the OSDs according to this pool’s PG distribution. Note that multiple pools could
be assigned to the same CRUSH hierarchy. Reweighting OSDs according to one pool’s distribution could
have unintended effects for other pools assigned to the same CRUSH hierarchy if they do not have the
same size (number of replicas) and PGs.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=weights-setting-osds-weight-by-placement-group-distribution](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=weights-setting-osds-weight-by-placement-group-distribution)*