# Creating CRUSH rules for replicated pools

> **Index:** `11.3.2.7.4`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=rules-creating-crush-replicated-pools](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=rules-creating-crush-replicated-pools)

---

## Creating CRUSH rules for replicated pools

Last Updated
: 2026-06-25

Use this information to create a CRUSH rule for a replicated pool from the
command-line.

To create a CRUSH rule for a replicated pool, run the following:

Syntax

```plaintext-ibm
ceph osd crush rule create-replicated NAME ROOT FAILURE_DOMAIN CLASS
```

Copy to clipboard

Where:

- `<name>`: The name of the rule.
- `<root>`: The root of the CRUSH hierarchy.
- `<failure-domain>`: The failure domain. For example: `host`
or `rack`.
- `<class>`: The storage device class. For example: `hdd` or
`ssd`.

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph osd crush rule create-replicated fast default host ssd
```

Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=rules-creating-crush-replicated-pools](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=rules-creating-crush-replicated-pools)*