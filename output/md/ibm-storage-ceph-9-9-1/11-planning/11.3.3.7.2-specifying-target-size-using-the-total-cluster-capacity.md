# Specifying target size using the total cluster capacity

> **Index:** `11.3.3.7.2`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=size-specifying-target-using-total-cluster-capacity](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=size-specifying-target-using-total-cluster-capacity)

---

## Specifying target size using the total cluster capacity

Last Updated
: 2026-06-25

Specify the target size of a pool by using the total cluster capacity.

1. Set the `target size` using the ratio of the total cluster capacity:

Syntax

```plaintext-ibm
 ceph osd pool set pool-name target_size_ratio ratio
```

Copy to clipboard

Example

```plaintext-ibm
 [ceph: root@host01 /]# ceph osd pool set mypool target_size_ratio 1.0
```

Copy to clipboard

tells the system that the pool `mypool` is expected to consume 1.0 relative to the
other pools with `target_size_ratio` set. If `mypool` is the only pool
in the cluster, this means an expected use of 100% of the total capacity. If there is a second pool
with `target_size_ratio` as 1.0, both pools would expect to use 50% of the cluster
capacity.

You can also set the target size of a pool at creation time by adding the optional
`--target-size-ratio <ratio>` argument to the `ceph osd pool
create` command.

**NOTE** If you specify impossible target size values, for example, a capacity larger than the
total cluster, or ratios that sum to more than 1.0, the cluster raises a
POOL_TARGET_SIZE_RATIO_OVERCOMMITTED or POOL_TARGET_SIZE_BYTES_OVERCOMMITTED health warning. If you
specify both target_size_ratio and target_size_bytes for a pool, the cluster considers only the
ratio, and raises a POOL_HAS_TARGET_SIZE_BYTES_AND_RATIO health warning.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=size-specifying-target-using-total-cluster-capacity](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=size-specifying-target-using-total-cluster-capacity)*