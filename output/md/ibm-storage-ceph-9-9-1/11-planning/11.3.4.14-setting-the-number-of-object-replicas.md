# Setting the number of object replicas

> **Index:** `11.3.4.14`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=overview-setting-number-object-replicas](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=overview-setting-number-object-replicas)

---

## Setting the number of object replicas

Last Updated
: 2026-06-25

Learn how to set the number of object replicas.

To set the number of object replicas on a replicated pool, run the following command:

Syntax

```plaintext-ibm
ceph osd pool set POOL_NAME size NUMBER_OF_REPLICAS
```

Copy to clipboard

You can run this command for each pool.

- The NUMBER_OF_REPLICAS parameter includes the object itself. If you want to
include the object and two copies of the object for a total of three instances of the object,
specify `3`.
Example

```plaintext-ibm
[ceph: root@host01 /]# ceph osd pool set data size 3
```

Copy to clipboard
- An object might accept I/O operations in degraded mode with fewer replicas than specified by the
`pool size` setting. To set a minimum number of required replicas for I/O, use the
`min_size` setting.
Example

```plaintext-ibm
ceph osd pool set data min_size 2
```

Copy to clipboard

This ensures
that no object in the data pool will receive I/O with fewer replicas than specified by the
`min_size` setting.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=overview-setting-number-object-replicas](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=overview-setting-number-object-replicas)*