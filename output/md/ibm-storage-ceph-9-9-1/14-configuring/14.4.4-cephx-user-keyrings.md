# Cephx user keyrings

> **Index:** `14.4.4`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-cephx-user-keyrings](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-cephx-user-keyrings)

---

## Cephx user keyrings

Last Updated
: 2026-06-25

When you run Ceph with authentication enabled, the `ceph` administrative
commands and Ceph clients require authentication keys to access the Ceph storage
cluster.

The most common way to provide these keys to the
`ceph`
administrative commands
and clients is to include a Ceph keyring under the
/etc/ceph/
directory. The
file name is usually
ceph.client.admin.keyring
or
$cluster.client.admin.keyring
. If you include the keyring under the
/etc/ceph/
directory, you do not need to specify a
`keyring`
entry in the Ceph configuration file.

> 📝 **Note:** Important:
Copy the
IBM Storage Ceph
cluster
keyring file to nodes where you will run administrative commands, because it contains the
`client.admin`
key.

To do so, run the following
command:

```plaintext-ibm
# scp USER@HOSTNAME:/etc/ceph/ceph.client.admin.keyring /etc/ceph/ceph.client.admin.keyring
```

Copy to clipboard

Replace
USER
with the user name used on the host with the
`client.admin`
key and
HOSTNAME
with the host name of that
host.

> 📝 **Note:** Note:
Ensure the
ceph.keyring
file has appropriate permissions set on the
client machine.

You can specify the key itself in the Ceph configuration file using the `key`
setting, which is not recommended, or a path to a key file using the `keyfile`
setting.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-cephx-user-keyrings](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-cephx-user-keyrings)*