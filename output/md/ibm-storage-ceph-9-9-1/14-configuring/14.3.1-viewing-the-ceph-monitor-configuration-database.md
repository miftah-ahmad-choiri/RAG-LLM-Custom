# Viewing the Ceph Monitor configuration database

> **Index:** `14.3.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-viewing-ceph-monitor-database](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-viewing-ceph-monitor-database)

---

## Viewing the Ceph Monitor configuration database

Last Updated
: 2026-06-25

You can view Ceph Monitor configuration in the configuration database.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Root-level access to a Ceph Monitor host.

### Procedure

1. Log into the `cephadm` shell.

```plaintext-ibm
[root@host01 ~]# cephadm shell
```

Copy to clipboard
2. Use the `ceph config` command to view the configuration database.

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config get mon
```

Copy to clipboard

For more information about the options available for the
`ceph config`
command,
use
`ceph config -h`
.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-viewing-ceph-monitor-database](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-viewing-ceph-monitor-database)*