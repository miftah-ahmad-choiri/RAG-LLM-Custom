# Ceph Monitor quorum

> **Index:** `14.3.3`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-ceph-monitor-quorum](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-ceph-monitor-quorum)

---

## Ceph Monitor quorum

Last Updated
: 2026-06-25

Run a production IBM Storage Ceph cluster with at least three Ceph Monitors to ensure
high availability. When you run multiple monitors, you can specify the initial monitors that must be
members of the storage cluster to establish a quorum. This may reduce the time it takes for the
storage cluster to come online.

A cluster will run sufficiently with a single monitor. However, a single monitor is a
single-point-of-failure. To ensure high availability in a production Ceph storage cluster, run Ceph
with multiple monitors so that the failure of a single monitor will not cause a failure of the
entire storage cluster.

When a Ceph storage cluster runs multiple Ceph Monitors for high availability, Ceph Monitors use
the Paxos algorithm to establish consensus about the master cluster map. A consensus requires a
majority of monitors running to establish a quorum for consensus about the cluster map. For example,
1; 2 out of 3; 3 out of 5; 4 out of 6; and so on.

```plaintext-ibm
[mon]
mon_initial_members = a,b,c
```

Copy to clipboard

> 📝 **Note:** Note:
Majority of the monitors in the storage cluster must be able to reach each other in to
establish a quorum. You can decrease the initial number of monitors to establish a quorum with the
`mon_initial_members`
option.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-ceph-monitor-quorum](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-ceph-monitor-quorum)*