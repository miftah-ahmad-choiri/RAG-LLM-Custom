# OSD reporting status

> **Index:** `14.7.5`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-osd-reporting-status](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-osd-reporting-status)

---

## OSD reporting status

Last Updated
: 2026-06-25

If a Ceph OSD Daemon does not report to a Ceph Monitor, the Ceph Monitor marks the Ceph
OSD Daemon `down` after the `mon_osd_report_timeout`, which is 900
seconds, elapses. A Ceph OSD Daemon sends a report to a Ceph Monitor when a reportable event such as
a failure, a change in placement group stats, a change in `up_thru` or when it boots
within 5 seconds. the minimum report interval can be changed.

You can change the Ceph OSD Daemon minimum report interval by setting the
`osd_mon_report_interval` value at runtime:

Syntax

```plaintext-ibm
ceph config set osd osd_mon_report_interval TIME_IN_SECONDS
```

Copy to clipboard

To get, set, and verify the config you can use the following example:

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph config get osd osd_mon_report_interval
5

[ceph: root@host01 /]# ceph config set osd osd_mon_report_interval
20

[ceph: root@host01 /]# ceph config dump | grep osd
global                  advanced  osd_pool_default_crush_rule                  -1
osd                   basic     osd_memory_target                            4294967296
osd                   advanced  osd_mon_report_interval                      20
```

Copy to clipboard

Figure 1.
Ceph configuration update

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-osd-reporting-status](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configuration-osd-reporting-status)*