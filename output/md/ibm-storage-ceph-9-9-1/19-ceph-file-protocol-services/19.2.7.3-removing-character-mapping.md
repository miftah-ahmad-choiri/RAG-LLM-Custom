# Removing character mapping

> **Index:** `19.2.7.3`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=folding-removing-character-mapping](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=folding-removing-character-mapping)

---

## Removing character mapping

Last Updated
: 2026-06-25

Learn how to  remove the `ceph.dir.charmap` attribute from a directory in
CephFS. If the attribute is not set, the directory has no character mapping. Character mapping is
inherited only at the time of directory creation. Removing the mapping from a directory does not
affect its parent or child directories.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- Directory must not be part of a snapshot. .
- Directory must be empty before character mapping can be removed.
- You must have appropriate authorization to modify extended attributes on the directory.

### Procedure

1. To remove the
`ceph.dir.charmap`
attribute and restore the default
behavior, run the
setfattr
command.
```plaintext-ibm
  setfattr -x ceph.dir.charmap foo/
```

Copy to clipboard
For example,

```
setfattr -x ceph.dir.charmap foo/
getfattr -n ceph.dir.charmap foo/
foo/: ceph.dir.charmap: No such attribute
```
2. To confirm that the character mapping was removed, run the following command.
```plaintext-ibm
getfattr -n ceph.dir.charmap <directory>/
```

Copy to clipboard
For example,
```
foo/: ceph.dir.charmap: No such attribute
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=folding-removing-character-mapping](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=folding-removing-character-mapping)*