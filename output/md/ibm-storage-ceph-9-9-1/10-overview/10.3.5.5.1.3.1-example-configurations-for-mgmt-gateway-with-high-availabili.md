# Example configurations for mgmt-gateway with high availability

> **Index:** `10.3.5.5.1.3.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=gateway-example-configurations-mgmt-high-availability](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=gateway-example-configurations-mgmt-high-availability)

---

## Example configurations for mgmt-gateway with high availability

Last Updated
: 2026-06-25

Use these examples to configure multiple `mgmt-gateway` instances with high availability (HA) in an active/standby configuration with keepalived for seamless failover.

```plaintext-ibm
service_type: mgmt-gateway
placement:
  label: mgmt
spec:
  enable_auth: true
  virtual_ip: 192.168.100.220  # HA Virtual IP
```

Copy to clipboard

### Example of ingress configuration for keepalived

```plaintext-ibm
service_type: ingress
service_id: ingress-mgmt-gw
placement:
  label: mgmt
virtual_ip: 192.168.100.220
backend_service: mgmt-gateway
keepalive_only: true
```

Copy to clipboard

### Example for deploying the mgmt-gateway in a high availability configuration

The following is a complete example that shows all the components required to deploy the Ceph management gateway in HA mode with SSO enabled. The configuration includes the nodes, the management gateway,
`oauth2-proxy`
, and ingress.

```plaintext-ibm
---
service_type: host
hostname: ceph-node-1
addr: 192.168.100.101
labels:
  - mgmt
---
service_type: host
hostname: ceph-node-2
addr: 192.168.100.102
labels:
  - mgmt
---
service_type: mgmt-gateway
placement:
  label: mgmt
spec:
  enable_auth: true
  virtual_ip: <your-virtual-ip>
---
service_type: oauth2-proxy
placement:
  label: mgmt
spec:
  provider_display_name: "<your-oidc-provider>"
  client_id: <your-client-id>
  client_secret: <your-client-secret>
  oidc_issuer_url: <your-oidc-url>
  allowlist_domains:
    - 192.168.100.1:8080
---
service_type: ingress
service_id: ingress-mgmt-gw
placement:
  label: mgmt
virtual_ip: <your-virtual-ip>
backend_service: mgmt-gateway
keepalive_only: true
```

Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=gateway-example-configurations-mgmt-high-availability](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=gateway-example-configurations-mgmt-high-availability)*