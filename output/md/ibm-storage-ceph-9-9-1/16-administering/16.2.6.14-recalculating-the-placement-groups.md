# Recalculating the placement groups

> **Index:** `16.2.6.14`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=osds-recalculating-placement-groups](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=osds-recalculating-placement-groups)

---

## Recalculating the placement groups

Last Updated
: 2026-06-25

Adjusting the number of placement groups can be done online. Recalculating is not only a
recalculation of the PG numbers, but will involve data relocation, which will be a lengthy process.
However, the data availability will be maintained at any time.

Placement groups (PGs) define the spread of any pool data across the available OSDs. A placement
group is built upon the given redundancy algorithm to be used. For a 3-way replication, the
redundancy is defined to use three different OSDs. For erasure-coded pools, the number of OSDs to
use is defined by the number of chunks.

When defining a pool the number of placement groups defines the grade of granularity the data is
spread with across all available OSDs. The higher the number the better the equalization of capacity
load can be. However, since handling the placement groups is also important in case of
reconstruction of data, the number is significant to be carefully chosen upfront. To support
calculation a tool is available to produce agile environments.

During the lifetime of a storage cluster, a pool can grow above the initially anticipated limits.
With the growing number of drives a recalculation is recommended. The number of placement groups per
OSD should be around 100. When adding more OSDs to the storage cluster the number of PGs per OSD
will lower over time. Starting with 120 drives initially in the storage cluster and setting the
`pg_num` of the pool to 4000 will end up in 100 PGs per OSD, given with the
replication factor of three. Over time, when growing to ten times the number of OSDs, the number of
PGs per OSD will go down to ten only. Because a small number of PGs per OSD will tend to an unevenly
distributed capacity, consider adjusting the PGs per pool.

Very high numbers of PGs per OSD should be avoided, because reconstruction of all PGs on a failed
OSD will start at once. A high number of IOPS is required to perform reconstruction in a timely
manner, which might not be available. This would lead to deep I/O queues and high latency rendering
the storage cluster unusable or will result in long healing times.

### Reference

- To calculate the values given by use case, see the [PG calculator](https://www.ibm.com/links?url=https%3A%2F%2Faccess.redhat.com%2Flabs%2Fcephpgc%2F).
- Erasure Code Pools

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=osds-recalculating-placement-groups](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=osds-recalculating-placement-groups)*