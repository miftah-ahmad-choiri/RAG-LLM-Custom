# Viewing centralized logs of the Ceph cluster

> **Index:** `16.1.2.3.11`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=dashboard-viewing-centralized-logs-ceph-cluster](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=dashboard-viewing-centralized-logs-ceph-cluster)

---

## Viewing centralized logs of the Ceph cluster

Last Updated
: 2026-06-25

Use the Ceph Dashboard to view logs from all the clients in a centralized space in the IBM Storage Ceph cluster. Centralized logs provide efficient monitoring by using Loki and alloy.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster.
- Dashboard is installed.
- Grafana is configured and the cluster is logged in to.

### About this task

Loki is a log aggregation system that is designed to store and query logs. Alloy is an agent that provides the contents of local logs to a private Grafana Loki instance.

### Procedure

1. From the dashboard navigation, go to
Administration
>
Services
.
2. From
Services
, click
Create
.
3. In the
Create Service
form, from the
Type
list, select
loki
.
4. Complete the rest of the form.
5. To save the service, click
Create Service
.
6. Repeat steps
2
through
5
to create the
`alloy`
service.
When creating the
`alloy`
service, select
alloy
from the
Type
list, instead of
loki
.

> 📝 **Note:** Note:
By default, the alloy service is deployed on all running hosts.

To see whether the services were created successfully, verify that
loki
and
alloy
services are running in the services list.
7. Enable logging to the file.

1. Go to
Administration
>
Configuration
.
2. From the search bar, search for
log_to_file
.

> 📝 **Note:** Note:
If the file does not show in the search results, clear any filter settings that might be selected.
3. Select
log_to_file
and click
Edit
.
The
Edit log_to_file
form opens.
4. In the
Values
section of the form, from the
global
list, select
true
.
5. Click
Update
.

  1. Go to
  Administration
  >
  Configuration
  .
  2. From the search bar, search for
  log_to_file
  .

  > 📝 **Note:** Note:
  If the file does not show in the search results, clear any filter settings that might be selected.
  3. Select
  log_to_file
  and click
  Edit
  .
  The
  Edit log_to_file
  form opens.
  4. In the
  Values
  section of the form, from the
  global
  list, select
  true
  .
  5. Click
  Update
  .
8. The
Updated config option log_to_file
notification displays and you are returned to the
Configuration
table.
9. Repeat step
7
to configure the
global
value to
true
for the
`mon_cluster_log_to_file`
file.
Search for
mon_cluster_log_to_file
instead of
`log_to_file`
.

> 📝 **Note:** Important:
Both the
`log_to_file`
and
`mon_cluster_log_to_file`
files need to be configured.
10. **Optional:**
To view the Ceph Object Gateway
ops_log
,
`rgw_enable_ops_log`
must be set to
`true`
by using the following command:

```plaintext-ibm
$ ceph config set client.rgw rgw_enable_ops_log true
```

Copy to clipboard

1. Go to
Administration
->
Configuration
.
2. Change level from
basic
to
Dev
.
3. Search for
`rgw_enable_ops_log`
and edit the value to
`true`
.
4. From the
Daemon Logs
tab, locate the logs file in the
filename
field and run the query to view the ops log.

  1. Go to
  Administration
  ->
  Configuration
  .
  2. Change level from
  basic
  to
  Dev
  .
  3. Search for
  `rgw_enable_ops_log`
  and edit the value to
  `true`
  .
  4. From the
  Daemon Logs
  tab, locate the logs file in the
  filename
  field and run the query to view the ops log.
11. View the centralized logs by going to
Cluster
>
Logs
and select the
Daemon Logs
tab.
The
Logs
view is displayed.

> 📝 **Note:** Note:
If the
Daemon Logs
tab has a general
Welcome to Grafana
page and does not display the
Logs
view, sign in to Grafana and reload the page.
12. Use the
Log browser
field to select the relevant file and label values.
Figure 1
shows an example of selecting the relevant labels and values for the search query.
Figure 1.
Using the
Log browser
13. Click
Show logs
to view the logs from the selected file.
Figure 2
shows the
Daemon Logs
view after a log query has been set.
Figure 2.
Viewing centralized logs

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=dashboard-viewing-centralized-logs-ceph-cluster](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=dashboard-viewing-centralized-logs-ceph-cluster)*