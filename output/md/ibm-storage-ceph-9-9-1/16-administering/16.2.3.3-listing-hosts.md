# Listing hosts

> **Index:** `16.2.3.3`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=hosts-listing](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=hosts-listing)

---

## Listing hosts

Last Updated
: 2026-06-25

> 📝 **Note:** Note:
The
*STATUS*
of the hosts is blank, in the output of the
`ceph orch host
ls`
command.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Hosts are added to the cluster.

### Procedure

1. Log into the `cephadm` shell:

Example

```plaintext-ibm
[root@host01 ~]# cephadm shell
```

Copy to clipboard
2. List the hosts of the cluster:

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch host ls
```

Copy to clipboard

You will see that the *STATUS* of the hosts is blank which is expected.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=hosts-listing](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=hosts-listing)*