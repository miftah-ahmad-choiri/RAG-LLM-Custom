# Placement group stale state

> **Index:** `16.1.2.2.2.12`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=monitoring-placement-group-stale-state](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=monitoring-placement-group-stale-state)

---

## Placement group stale state

Last Updated
: 2026-06-25

If the Primary OSD of a placement group’s acting set fails to report to the monitor or if
other OSDs have reported the primary OSD `down`, the monitors will mark the placement
group `stale`.

While Ceph uses heartbeats to ensure that hosts and daemons are running, the
`ceph-osd` daemons may also get into a `stuck` state where they are
not reporting statistics in a timely manner. For example, a temporary network fault. By default, OSD
daemons report their placement group, up through boot and failure statistics every half a second,
that is, `0.5`, which is more frequent than the heartbeat thresholds.

When you start the storage cluster, it is common to see the `stale` state until
the peering process completes. After the storage cluster has been running for a while, seeing
placement groups in the `stale` state indicates that the primary OSD for those
placement groups is `down` or not reporting placement group statistics to the
monitor.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=monitoring-placement-group-stale-state](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=monitoring-placement-group-stale-state)*