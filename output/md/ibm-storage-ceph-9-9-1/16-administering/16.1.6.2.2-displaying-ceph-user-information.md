# Displaying Ceph user information

> **Index:** `16.1.6.2.2`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=users-displaying-ceph-user-information](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=users-displaying-ceph-user-information)

---

## Displaying Ceph user information

Last Updated
: 2026-06-25

You can display a Ceph’s user information using the command-line interface.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Root-level access to the node.

### Procedure

1. To retrieve a specific user, key and capabilities, run the following:

Syntax

```plaintext-ibm
ceph auth export TYPE.ID
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph auth export mgr.host02.eobuuv
```

Copy to clipboard
2. You can also use the `-o FILE_NAME` option.

Syntax

```plaintext-ibm
ceph auth export TYPE.ID -o FILE_NAME
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph auth export osd.9 -o filename
export auth(key=AQBV7U5g1XDULhAAKo2tw6ZhH1jki5aVui2v7g==)
```

Copy to clipboard

The `auth export` command is identical to `auth get`, but also
prints out the internal `auid`, which isn’t relevant to end users.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=users-displaying-ceph-user-information](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=users-displaying-ceph-user-information)*