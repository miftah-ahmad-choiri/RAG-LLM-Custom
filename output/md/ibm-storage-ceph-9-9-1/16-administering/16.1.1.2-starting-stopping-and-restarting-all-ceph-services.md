# Starting, stopping, and restarting all Ceph services

> **Index:** `16.1.1.2`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=management-starting-stopping-restarting-all-ceph-services](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=management-starting-stopping-restarting-all-ceph-services)

---

## Starting, stopping, and restarting all Ceph services

Last Updated
: 2026-06-25

You can start, stop, and restart all Ceph services from the host where you want to manage the Ceph services.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- Have root-node access
- Log into the `cephadm` shell.
          For example,

```
[root@host01 ~]# cephadm shell
```

### About this task

Ceph services are logical groups of Ceph daemons of the same type, configured to run in the same
IBM Storage Ceph
cluster. The orchestration layer in Ceph allows the user to manage these services in a centralized way, making it easy to run operations that affect all the Ceph daemons that belong to the same logical service. The Ceph daemons running in each host are managed through the Systemd service.

> 📝 **Note:** Important:
To start, stop, or restart a specific Ceph daemon in a specific host, you need to use the SystemD service. To obtain a list of the SystemD services running in a specific host, connect to the host, and run the following command:

```plaintext-ibm
[root@host01 ~]# systemctl list-units “ceph*”
```

Copy to clipboard

The output provides a list of the service names that you can use to manage each Ceph daemon.

### Get a list of Ceph services

#### Procedure

Get a list of Ceph services configured in the
IBM Storage Ceph
cluster and to get the specific service ID.

```plaintext-ibm
ceph orch ls
```

Copy to clipboard

For example,

```
[ceph: root@host01 /]# ceph orch ls

NAME                       RUNNING  REFRESHED  AGE  PLACEMENT  IMAGE NAME                                            IMAGE ID
alertmanager                   1/1  4m ago     4M   count:1    cp.icr.io/cp/ibm-ceph/prometheus-alertmanager:v4.15   b7bae610cd46
crash                          3/3  4m ago     4M   *          cp.icr.io/cp/ibm-ceph/ceph-9-rhel9:latest             c88a5d60f510
grafana                        1/1  4m ago     4M   count:1    cp.icr.io/cp/ibm-ceph/grafana-rhel9:latest            bd3d7748747b
mgr                            2/2  4m ago     4M   count:2    cp.icr.io/cp/ibm-ceph/ceph-9-rhel9:latest             c88a5d60f510
mon                            2/2  4m ago     10w  count:2    cp.icr.io/cp/ibm-ceph/ceph-9-rhel9:latest             c88a5d60f510
node-exporter                  1/3  4m ago     4M   *          cp.icr.io/cp/ibm-ceph/prometheus-node-exporter:v4.15  mix
osd.all-available-devices      5/5  4m ago     3M   *          cp.icr.io/cp/ibm-ceph/ceph-9-rhel9:latest             c88a5d60f510
prometheus                     1/1  4m ago     4M   count:1    cp.icr.io/cp/ibm-ceph/prometheus:v4.15                bebb0ddef7f0
rgw.test_realm.test_zone       2/2  4m ago     3M   count:2    cp.icr.io/cp/ibm-ceph/ceph-9-rhel9:latest             c88a5d60f510
```

### Start a service

#### Procedure

Start a specific service by running the
ceph orch start SERVICE_ID
command.
For example,

```
[ceph: root@host01 /]# ceph orch start node-exporter
```

### Stop a service

#### Procedure

Stop a specific service by running the following command.

> 📝 **Note:** Important:
The
ceph orch stop SERVICE_ID
command results in the
IBM Storage Ceph
cluster being inaccessible, only for the MON and MGR service. It is recommended to use the
systemctl stop SERVICE_ID
command to stop a specific daemon in the host.

For more information, see Starting, stopping, and restarting all Ceph daemons using the systemctl command.

```plaintext-ibm
ceph orch stop SERVICE_ID
```

Copy to clipboard

In the following example the
ceph orch stop node-exporter
command removes all the daemons of the
`node exporter`
service.

```
[ceph: root@host01 /]# ceph orch stop node-exporter
```

### Restart a service

#### Procedure

Restart a specific service by running the following command.

```plaintext-ibm
ceph orch restart SERVICE_ID
```

Copy to clipboard

For example,

```
[ceph: root@host01 /]# ceph orch restart node-exporter
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=management-starting-stopping-restarting-all-ceph-services](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=management-starting-stopping-restarting-all-ceph-services)*