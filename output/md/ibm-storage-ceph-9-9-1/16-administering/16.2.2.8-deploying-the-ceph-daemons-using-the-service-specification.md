# Deploying the Ceph daemons using the service specification

> **Index:** `16.2.2.8`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=services-deploying-ceph-daemons-using-service-specification](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=services-deploying-ceph-daemons-using-service-specification)

---

## Deploying the Ceph daemons using the service specification

Last Updated
: 2026-06-25

Using the Ceph Orchestrator, you can deploy daemons such as Ceph Manager, Ceph Monitors, Ceph OSDs, monitoring stack, and others using the service specification in a YAML file.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Root-level access to all the nodes.

### Procedure

1. Create the `yaml` file:

Example

```plaintext-ibm
[root@host01 ~]# touch mon.yaml
```

Copy to clipboard
2. This file can be configured in two different ways:

- Edit the file to include the host details in placement specification:

Syntax

```plaintext-ibm
service_type: SERVICE_NAME
placement:
  hosts:
    - HOST_NAME_1
    - HOST_NAME_2
```

Copy to clipboard

Example

```plaintext-ibm
service_type: mon
placement:
  hosts:
    - host01
    - host02
    - host03
```

Copy to clipboard
- Edit the file to include the label details in placement specification:

Syntax

```plaintext-ibm
service_type: SERVICE_NAME
placement:
  label: "LABEL_1"
```

Copy to clipboard

Example

```plaintext-ibm
service_type: mon
placement:
  label: "mon"
```

Copy to clipboard

  - Edit the file to include the host details in placement specification:


  Syntax


  ```plaintext-ibm
  service_type: SERVICE_NAME
  placement:
    hosts:
      - HOST_NAME_1
      - HOST_NAME_2
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  service_type: mon
  placement:
    hosts:
      - host01
      - host02
      - host03
  ```



  Copy to clipboard
  - Edit the file to include the label details in placement specification:


  Syntax


  ```plaintext-ibm
  service_type: SERVICE_NAME
  placement:
    label: "LABEL_1"
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  service_type: mon
  placement:
    label: "mon"
  ```



  Copy to clipboard
3. Optional: Use extra container arguments in the service specification files such as CPUs, CA certificates, and other files while deploying services. Use extra arguments to enable additional metrics in node-exporter deployed by `cephadm`.

Example

```plaintext-ibm
extra_container_args:
     - "-v"
     - "/etc/pki/ca-trust/extracted:/etc/pki/ca-trust/extracted:ro"
     - "--security-opt"
     - "label=disable"
     - "cpus=2"
     - "--collector.textfile.directory=/var/lib/node_exporter/textfile_collector2"
```

Copy to clipboard
4. Mount the YAML file under a directory in the container:

Example

```plaintext-ibm
[root@host01 ~]# cephadm shell --mount mon.yaml:/var/lib/ceph/mon/mon.yaml
```

Copy to clipboard
5. Navigate to the directory:

Example

```plaintext-ibm
[ceph: root@host01 /]# cd /var/lib/ceph/mon/
```

Copy to clipboard
6. Deploy the Ceph daemons using service specification:

Syntax

```plaintext-ibm
ceph orch apply -i FILE_NAME.yaml
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 mon]# ceph orch apply -i mon.yaml
```

Copy to clipboard

### Verification

- List the service:

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch ls
```

Copy to clipboard
- List the hosts, daemons, and processes:

Syntax

```plaintext-ibm
ceph orch ps --daemon_type=DAEMON_NAME
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch ps --daemon_type=mon
```

Copy to clipboard

### Reference

- Listing hosts

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=services-deploying-ceph-daemons-using-service-specification](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=services-deploying-ceph-daemons-using-service-specification)*