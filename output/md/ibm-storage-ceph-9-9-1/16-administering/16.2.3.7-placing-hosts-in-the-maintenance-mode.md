# Placing hosts in the maintenance mode

> **Index:** `16.2.3.7`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=hosts-placing-in-maintenance-mode](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=hosts-placing-in-maintenance-mode)

---

## Placing hosts in the maintenance mode

Last Updated
: 2026-06-25

You can use the Ceph Orchestrator to place the hosts in and out of the maintenance
mode.

The `ceph orch host maintenance enter` command stops the `systemd
target` which causes all the Ceph daemons to stop on the host. Similarly, the `ceph
orch host maintenance exit` command restarts the `systemd target` and the
Ceph daemons restart on their own.

The orchestrator adopts the following workflow when the host is placed in maintenance:

1. Confirms the removal of hosts does not impact data availability by running the `orch host
ok-to-stop` command.
2. If the host has Ceph OSD daemons, it applies `noout` to the host subtree to
prevent data migration from triggering during the planned maintenance slot.
3. Stops the Ceph target, thereby, stopping all the daemons.
4. Disables the `ceph target` on the host, to prevent a reboot from automatically
starting Ceph services.

Exiting maintenance reverses this workflow sequence.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Root-level access to all the nodes.
- Hosts added to the cluster.

### Procedure

1. Log into the `cephadm` shell.

Example

```plaintext-ibm
[root@host01 ~]# cephadm shell
```

Copy to clipboard
2. You can either place the host in maintenance mode or place it out of the maintenance mode:

- Place the host in maintenance mode.

Syntax

```plaintext-ibm
ceph orch host maintenance enter HOST_NAME [--force]
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch host maintenance enter host02 --force
```

Copy to clipboard

The `--force` flag allows the user to bypass warnings, but not alerts.
- Place the host out of the maintenance mode.

Syntax

```plaintext-ibm
ceph orch host maintenance exit HOST_NAME
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch host maintenance exit host02
```

Copy to clipboard

  - Place the host in maintenance mode.


  Syntax


  ```plaintext-ibm
  ceph orch host maintenance enter HOST_NAME [--force]
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  [ceph: root@host01 /]# ceph orch host maintenance enter host02 --force
  ```



  Copy to clipboard


  The `--force` flag allows the user to bypass warnings, but not alerts.
  - Place the host out of the maintenance mode.


  Syntax


  ```plaintext-ibm
  ceph orch host maintenance exit HOST_NAME
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  [ceph: root@host01 /]# ceph orch host maintenance exit host02
  ```



  Copy to clipboard

### Verification

- List the hosts.

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch host ls
```

Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=hosts-placing-in-maintenance-mode](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=hosts-placing-in-maintenance-mode)*