# Removing a Ceph Monitor from an unhealthy storage cluster

> **Index:** `16.2.4.7`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=monitors-removing-ceph-monitor-from-unhealthy-storage-cluster](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=monitors-removing-ceph-monitor-from-unhealthy-storage-cluster)

---

## Removing a Ceph Monitor from an unhealthy storage cluster

Last Updated
: 2026-06-25

You can remove a `ceph-mon` daemon from an unhealthy storage cluster. An
unhealthy storage cluster is one that has placement groups persistently in not `active +
clean` state.

### Prerequisites

- Root-level access to the Ceph Monitor node.
- At least one running Ceph Monitor node.

### Procedure

1. Identify a surviving monitor and log into the host:

Syntax

```plaintext-ibm
ssh root@MONITOR_ID
```

Copy to clipboard

Example

```plaintext-ibm
[root@admin ~]# ssh root@host00
```

Copy to clipboard
2. Log in to each Ceph Monitor host and stop all the Ceph Monitors:

Syntax

```plaintext-ibm
cephadm unit --name DAEMON_NAME.HOSTNAME stop
```

Copy to clipboard

Example

```plaintext-ibm
[root@host00 ~]# cephadm unit --name mon.host00 stop
```

Copy to clipboard
3. Set up the environment suitable for extended daemon maintenance and to run the daemon
interactively:

Syntax

```plaintext-ibm
cephadm shell --name DAEMON_NAME.HOSTNAME
```

Copy to clipboard

Example

```plaintext-ibm
[root@host00 ~]# cephadm shell --name mon.host00
```

Copy to clipboard
4. Extract a copy of the `monmap` file:

Syntax

```plaintext-ibm
ceph-mon -i HOSTNAME --extract-monmap TEMP_PATH
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host00 /]# ceph-mon -i host01 --extract-monmap /tmp/monmap

2022-01-05T11:13:24.440+0000 7f7603bd1700 -1 wrote monmap to /tmp/monmap
```

Copy to clipboard
5. Remove the non-surviving Ceph Monitor(s):

Syntax

```plaintext-ibm
monmaptool TEMPORARY_PATH --rm HOSTNAME
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host00 /]# monmaptool /tmp/monmap --rm host01
```

Copy to clipboard
6. Inject the surviving monitor map with the removed monitor(s) into the surviving Ceph Monitor:

Syntax

```plaintext-ibm
ceph-mon -i HOSTNAME --inject-monmap TEMP_PATH
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host00 /]# ceph-mon -i host00 --inject-monmap /tmp/monmap
```

Copy to clipboard
7. Start only the surviving monitors:

Syntax

```plaintext-ibm
cephadm unit --name DAEMON_NAME.HOSTNAME start
```

Copy to clipboard

Example

```plaintext-ibm
[root@host00 ~]# cephadm unit --name mon.host00 start
```

Copy to clipboard
8. Verify the monitors form a quorum:

Example

```plaintext-ibm
[ceph: root@host00 /]# ceph -s
```

Copy to clipboard
9. Optional: Archive the removed Ceph Monitor’s data directory in
`/var/lib/ceph/CLUSTER_FSID/mon.HOSTNAME` directory.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=monitors-removing-ceph-monitor-from-unhealthy-storage-cluster](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=monitors-removing-ceph-monitor-from-unhealthy-storage-cluster)*