# Configuring high availability for NFS service

> **Index:** `16.2.11.5.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=servers-configuring-high-availability-nfs-service](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=servers-configuring-high-availability-nfs-service)

---

## Configuring high availability for NFS service

Last Updated
: 2026-09-10

Configure NFS gateways in
IBM Storage Ceph
in multi-active or active-passive mode to provide a highly available NFS service by using ingress services and virtual IP addresses.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

> 📝 **Note:** Note:
The NFS service and ingress service run on different hosts. For optimal performance and high availability, configure each service on separate hosts.

- A running IBM Storage Ceph cluster.
- Hosts are added to the cluster, with roles identified: NFS servers or NFS daemons.
- The NFS module is enabled.

### About this task

An *ingress service* provides external access to internal services within a Kubernetes cluster. It uses routing rules defined in ingress resources to direct HTTP and HTTPS traffic to the appropriate backend services.

When a cluster is created with
--ingress
flag, an ingress service is also deployed to provide high-availability for the NFS servers. This applies only to Ceph clusters. A virtual IP is used to provide a known, stable NFS endpoint that all NFS clients can use to mount. Ceph handles the details of redirecting NFS traffic on the virtual IP to the appropriate backend NFS servers and redeploys NFS servers when they fail.

> 📝 **Note:** Important:
When an
ingress
service is deployed on the NFS cluster, the backend NFS service servers see the HAProxy’s IP address and not the client’s IP address. As a result, if you are restricting client access based on IP address, access restrictions for NFS shares will not work as expected.

> 📝 **Note:** Note:

- If the active NFS server serving a client becomes unavailable, the client's I/O operations are interrupted until a replacement NFS server is available and the NFS cluster resumes operation.
- NFS transparent state migration, available as a Technology Preview feature, maintains client connection state across NFS server instances in multi-active deployments. When an NFS server becomes unavailable, a peer NFS server can continue processing client requests with minimal disruption. For more information, see NFS transparent state migration (Technology Preview).

Deploying an ingress service for an existing service provides:

- A stable, virtual IP that can be used to access the NFS server.
- Failover between hosts when the hosts fail.
- Load distribution across multiple NFS server gateways when necessary.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=servers-configuring-high-availability-nfs-service](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=servers-configuring-high-availability-nfs-service)*