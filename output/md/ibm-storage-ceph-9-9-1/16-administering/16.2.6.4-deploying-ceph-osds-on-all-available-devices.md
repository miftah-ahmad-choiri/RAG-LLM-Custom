# Deploying Ceph OSDs on all available devices

> **Index:** `16.2.6.4`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=osds-deploying-ceph-all-available-devices](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=osds-deploying-ceph-all-available-devices)

---

## Deploying Ceph OSDs on all available devices

Last Updated
: 2026-06-25

You can deploy all OSDS on all the available devices. `cephadm` allows the Ceph
Orchestrator to discover and deploy the OSDs on any available and unused storage device.

To deploy OSDs all available devices, run the command without the unmanaged
parameter and then re-run the command with the parameter to prevent from creating future OSDs.

> 📝 **Note:** Note:
The deployment of OSDs with
`--all-available-devices`
is generally used for
smaller clusters. For larger clusters, use the OSD specification file.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Hosts are added to the cluster.
- All manager and monitor daemons are deployed.

### Procedure

1. Log into the `cephadm` shell:

Example

```plaintext-ibm
[root@host01 ~]# cephadm shell
```

Copy to clipboard
2. List the available devices to deploy OSDs:

Syntax

```plaintext-ibm
ceph orch device ls [--hostname=HOSTNAME_1 HOSTNAME_2] [--wide] [--refresh]
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch device ls --wide --refresh
```

Copy to clipboard
3. Deploy OSDs on all available devices:

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch apply osd --all-available-devices
```

Copy to clipboard

The effect of `ceph orch apply` is persistent which means that the Orchestrator
automatically finds the device, adds it to the cluster, and creates new OSDs. This occurs under the
following conditions:

- New disks or drives are added to the system.
- Existing disks or drives are zapped.
- An OSD is removed and the devices are zapped.

You can disable automatic creation of OSDs on all the available devices by using the
`--unmanaged` parameter.

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch apply osd --all-available-devices --unmanaged=true
```

Copy to clipboard

Setting the parameter `--unmanaged` to `true` disables the creation
of OSDs and also there is no change if you apply a new OSD service.

> 📝 **Note:** Note:
The command
`ceph orch daemon add`
creates new OSDs, but does not add an OSD
service.

  - New disks or drives are added to the system.
  - Existing disks or drives are zapped.
  - An OSD is removed and the devices are zapped.


  You can disable automatic creation of OSDs on all the available devices by using the
  `--unmanaged` parameter.


  Example


  ```plaintext-ibm
  [ceph: root@host01 /]# ceph orch apply osd --all-available-devices --unmanaged=true
  ```



  Copy to clipboard


  Setting the parameter `--unmanaged` to `true` disables the creation
  of OSDs and also there is no change if you apply a new OSD service.


  > 📝 **Note:** Note:
  The command
  `ceph orch daemon add`
  creates new OSDs, but does not add an OSD
  service.

### Verification

- List the service:

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph orch ls
```

Copy to clipboard
- View the details of the node and devices:

Example

```plaintext-ibm
[ceph: root@host01 /]# ceph osd tree
```

Copy to clipboard

### Reference

- Listing devices for Ceph OSD deployment

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=osds-deploying-ceph-all-available-devices](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=osds-deploying-ceph-all-available-devices)*