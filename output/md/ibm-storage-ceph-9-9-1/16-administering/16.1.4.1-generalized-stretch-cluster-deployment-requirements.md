# Generalized stretch cluster deployment requirements

> **Index:** `16.1.4.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=zones-generalized-stretch-cluster-deployment-requirements](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=zones-generalized-stretch-cluster-deployment-requirements)

---

## Generalized stretch cluster deployment requirements

Last Updated
: 2026-06-25

This information details important hardware, software, and network requirements that are needed for deploying a generalized stretch cluster configuration for three availability zones.

Use the following minimum hardware requirements before deploying generalized stretch cluster configuration for three availability zones.
Table 1
lists the physical server locations and Ceph component layout for an example three availability zone deployment.

| Host name | Datacenter | Ceph services |
| --- | --- | --- |
| host01 | DC1 | OSD+MON+MGR |
| host02 | DC1 | OSD+MON+MGR+RGW |
| host03 | DC1 | OSD+MON+MDS |
| host04 | DC2 | OSD+MON+MGR |
| host05 | DC2 | OSD+MON+MGR+RGW |
| host06 | DC2 | OSD+MON+MDS |
| host07 | DC3 | OSD+MON+MGR |
| host08 | DC3 | OSD+MON+MGR+RGW |
| host09 | DC3 | OSD+MON+MDS |

### Network configuration requirements

Use the following network configuration requirements before deploying generalized stretch cluster configuration for three availability zones.

- Have two separate networks, one public network and one cluster network.
- Have three different data centers that support VLANS and subnets for Ceph cluster and public networks for all data centers.

> 📝 **Note:** Note:
You can use different subnets for each of the data centers.
- The latencies between data centers running the IBM Storage Ceph Object Storage Devices (OSDs) cannot exceed 10 ms RTT.

For more information about network considerations, see Network considerations for IBM Storage Ceph.

### Cluster setup requirements

Ensure that the hostname is configured by using the bare or short hostname in all hosts.

```plaintext-ibm
hostnamectl set-hostname SHORT_NAME
```

Copy to clipboard

> 📝 **Note:** Important:
The
hostname
command should only return the short hostname, when run on all nodes. If the FQDN is returned, the cluster configuration will not be successful.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=zones-generalized-stretch-cluster-deployment-requirements](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=zones-generalized-stretch-cluster-deployment-requirements)*