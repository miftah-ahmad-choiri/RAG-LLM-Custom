# Deploying the monitoring stack

> **Index:** `16.2.7.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=stack-deploying-monitoring](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=stack-deploying-monitoring)

---

## Deploying the monitoring stack

Last Updated
: 2026-06-25

You can deploy the monitoring stack using the service specification in YAML file format.
All the monitoring services can have the network and port they bind to configured in the YAML
file.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster.
- Root-level access to the nodes.

### About this task

Deploy the monitoring stack using the Ceph orchestrator. The monitoring stack consists of
Prometheus, Prometheus exporters, Prometheus Alertmanager, Grafana, and Ceph Exporter. Ceph
Dashboard makes use of these components to store and visualize detailed metrics on cluster usage and
performance.

### Procedure

1. Enable the Prometheus module in the Ceph Manager daemon. This exposes the internal Ceph
metrics so that Prometheus can read them.
```plaintext-ibm
ceph mgr module enable prometheus
```

Copy to clipboard

> 📝 **Note:** Important:
Ensure this command is run before Prometheus is deployed. If the command was
not run before the deployment, you must redeploy Prometheus to update the
configuration.

```plaintext-ibm
ceph orch redeploy prometheus
```

Copy to clipboard
2. Navigate to the following directory.
```plaintext-ibm
cd /var/lib/ceph/DAEMON_PATH/
```

Copy to clipboard
For example,

```
[ceph: root@host01 mds/]# cd /var/lib/ceph/monitoring/
```

> 📝 **Note:** Note:

> 📝 **Note:** Note:
If the directory
`monitoring`
does not exist, create it.
3. Create the
`monitoring.yml`
file.
```plaintext-ibm
touch monitoring.yml
```

Copy to clipboard
4. Edit the specification file with a content similar to the following
example.For example,

```
service_type: prometheus
service_name: prometheus
placement:
  hosts:
  - host01
networks:
- 192.169.142.0/24
---
service_type: node-exporter
---
service_type: alertmanager
service_name: alertmanager
placement:
  hosts:
  - host01
networks:
- 192.169.142.0/24
---
service_type: grafana
service_name: grafana
placement:
  hosts:
  - host01
networks:
- 192.169.142.0/24
---
service_type: ceph-exporter
```

> 📝 **Note:** Important:
Ensure the monitoring stack components
alertmanager
,
prometheus
, and
grafana
are deployed on the same host. The
node-exporter
and
ceph-exporter
components should be deployed on all the hosts.
5. Apply monitoring services.
```plaintext-ibm
ceph orch apply -i MONITORING_STACK_FILE.yml
```

Copy to clipboard
For
example,

```
[ceph: root@host01 monitoring]# ceph orch apply -i monitoring.yml
```

### What to do next

> 📝 **Note:** Important:
Prometheus, Grafana, and the Ceph dashboard are all automatically configured
to talk to each other, resulting in a fully functional Grafana integration in the Ceph
dashboard.

- List the service.

```plaintext-ibm
ceph orch ls
```

Copy to clipboard
- List the hosts, daemons, and
processes.

```plaintext-ibm
ceph orch ps --service_name=SERVICE_NAME
```

Copy to clipboard
For
example,
```
[ceph: root@host01 /]# ceph orch ps --service_name=prometheus
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=stack-deploying-monitoring](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=stack-deploying-monitoring)*