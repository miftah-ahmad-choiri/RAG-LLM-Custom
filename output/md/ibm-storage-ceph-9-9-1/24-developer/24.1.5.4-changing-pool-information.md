# Changing pool information

> **Index:** `24.1.5.4`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configurations-changing-pool-information](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configurations-changing-pool-information)

---

## Changing pool information

Last Updated
: 2026-09-06

Understand how to use the RESTful plug-in to change pool information.

On the command line, run the following command:

```plaintext-ibm
echo -En '{"=OPTION": VALUE}' | curl --request PATCH --data @- --silent --user USER 'https://CEPH_MANAGER:8080/api/pool/ID'
```

Copy to clipboard

Replace:

- OPTION with the option to modify.
- VALUE with the new value of the option.
- USER with the username.
- CEPH_MANAGER with the IP address or short hostname of the node with the
active `ceph-mgr` instance.
- ID with the ID of the pool listed in the `pool` field.

Enter the user’s password when prompted.

> 📝 **Note:** Note:
If using a self-signed certificate, use the
--insecure
option.

```plaintext-ibm
echo -En '{"=OPTION": VALUE}' | curl --request PATCH --data @- --silent --insecure --user USER 'https://CEPH_MANAGER:8080/api/pool/ID'
```

Copy to clipboard

### Changing pool information using Python

In the Python interpreter, enter the following:

```plaintext-ibm
$ python
>> import requests
>> result = requests.patch('https://CEPH_MANAGER:8080/api/pool/ID', json={"OPTION": VALUE}, auth=("USER", "PASSWORD"))
>> print(result.json())
```

Copy to clipboard

Replace:

- CEPH_MANAGER with the IP address or short hostname of the node with the
active `ceph-mgr` instance.
- ID with the ID of the pool listed in the `pool` field.
- OPTION with one of the modifying options: pause,
noup, nodown, noout, noin,
nobackfill, norecover, noscrub, or
nodeep-scrub.
- VALUE with true or false.
- USER with the username.
- PASSWORD with the user’s password.

> 📝 **Note:** Note:
If using a self-signed certificate, use
`verify=False`
.

```plaintext-ibm
$ python
>> import requests
>> result = requests.patch('https://CEPH_MANAGER:8080/api/pool/ID', json={"OPTION": VALUE}, auth=("USER", "PASSWORD"), verify=False)
>> print(result.json())
```

Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configurations-changing-pool-information](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=configurations-changing-pool-information)*