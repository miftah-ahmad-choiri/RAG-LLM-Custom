# Fetching absolute path of a file system subvolume

> **Index:** `20.10.2.4`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-fetching-absolute-path-file-system-subvolume](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-fetching-absolute-path-file-system-subvolume)

---

## Fetching absolute path of a file system subvolume

Last Updated
: 2026-06-25

Learn how to fetch the absolute path of a Ceph File System (CephFS)
subvolume.

### Before you begin

Be sure that you have a CephFS subvolume.

### Procedure

Fetch the absolute path of the CephFS subvolume.

> 📝 **Note:** Note:
The
`--group_name`
parameter is optional. If the subvolume needs to be fetched
from within a subvolume group, then
`--group_name`
needs to be passed in the
command.

```plaintext-ibm
ceph fs subvolume getpath VOLUME_NAME SUBVOLUME_NAME [--group_name SUBVOLUME_GROUP_NAME]
```

Copy to clipboard

For
example,

```
[root@mon ~]# ceph fs subvolume getpath cephfs01 sub0 --group_name subgroup0

/volumes/subgroup0/sub0/81992fd4-9765-420e-85f9-2545d31ff136
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-fetching-absolute-path-file-system-subvolume](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-fetching-absolute-path-file-system-subvolume)*