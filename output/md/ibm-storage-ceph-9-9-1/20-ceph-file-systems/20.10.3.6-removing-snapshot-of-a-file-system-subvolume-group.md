# Removing snapshot of a file system subvolume group

> **Index:** `20.10.3.6`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=groups-removing-snapshot-file-system-subvolume-group](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=groups-removing-snapshot-file-system-subvolume-group)

---

## Removing snapshot of a file system subvolume group

Last Updated
: 2026-06-25

Learn how to remove snapshots of a Ceph File System (CephFS) subvolume group.

### Before you begin

Be sure that you have a CephFS subvolume group and snapshots of a subvolume group.

### About this task

> 📝 **Note:** Note:
Using the
`--force`
flag allows the command to succeed that would otherwise
fail if the snapshot did not exist.

### Procedure

Remove the snapshot of the CephFS subvolume group.

```plaintext-ibm
ceph fs subvolumegroup snapshot rm VOLUME_NAME GROUP_NAMESNAP_NAME  [--force]
```

Copy to clipboard

For
example:

```
[ceph: root@host01 /]# ceph fs subvolumegroup snapshot rm cephfs01 subgroup0 snap0 --force
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=groups-removing-snapshot-file-system-subvolume-group](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=groups-removing-snapshot-file-system-subvolume-group)*