# Creating a file system subvolume group

> **Index:** `20.10.3.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=groups-creating-file-system-subvolume-group](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=groups-creating-file-system-subvolume-group)

---

## Creating a file system subvolume group

Last Updated
: 2026-06-25

Learn how to create a Ceph File System (CephFS) subvolume group.

### About this task

> 📝 **Note:** Note:

- When creating a subvolume group, you can specify its data pool layout, UID, GID, and file mode
in octal numerals. By default, the subvolume group is created with an octal file mode ‘755’, uid
‘0’, gid ‘0’, and data pool layout of its parent directory.
- To set quotas while creating a subvolume group, see Setting and managing quotas on a file system subvolume group.

### Procedure

Create a CephFS subvolume group.

```plaintext-ibm
ceph fs subvolumegroup create VOLUME_NAME GROUP_NAME [--pool_layout DATA_POOL_NAME --uid UID --gid GID --mode OCTAL_MODE]
```

Copy to clipboard

For
example,

```
[ceph: root@host01 /]# ceph fs subvolumegroup create cephfs01 subgroup0
```

The
command succeeds even if the subvolume group already exists.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=groups-creating-file-system-subvolume-group](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=groups-creating-file-system-subvolume-group)*