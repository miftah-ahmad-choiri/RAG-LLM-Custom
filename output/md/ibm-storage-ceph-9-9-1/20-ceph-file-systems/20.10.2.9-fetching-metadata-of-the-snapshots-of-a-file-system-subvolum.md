# Fetching metadata of the snapshots of a file system subvolume

> **Index:** `20.10.2.9`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-fetching-metadata-snapshots-file-system-subvolume](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-fetching-metadata-snapshots-file-system-subvolume)

---

## Fetching metadata of the snapshots of a file system subvolume

Last Updated
: 2026-06-25

Learn how to list the snapshots of a Ceph File system (CephFS) subvolume.

### Before you begin

Be sure that you have a CephFS subvolume and snapshots of the subvolume.

### Procedure

Fetch the metadata of the snapshots of a CephFS subvolume.

> 📝 **Note:** Note:
The
`--group_name`
parameter is optional. If the subvolume is present within a
subvolume group, then
`--group_name`
needs to be passed in the command.

```plaintext-ibm
ceph fs subvolume snapshot info VOLUME_NAME SUBVOLUME_NAME SNAP_NAME [--group_name SUBVOLUME_GROUP_NAME]
```

Copy to clipboard

For
example:

```
[root@mon ~]# ceph fs subvolume snapshot info cephfs01 sub0 snap0 --group_name subgroup0
```

Example
output:

```
{
    "created_at": "2022-05-09 06:18:47.330682",
    "data_pool": "cephfs_data",
    "has_pending_clones": "no"
}
```

The output format is JSON and contains the following fields:

- `created_at:` time of creation of snapshot in the format `"YYYY-MM-DD
HH:MM:SS:ffffff"`.
- `data_pool:` data pool the snapshot belongs to.
- `has_pending_clones:`
"yes" if snapshot clone is in progress otherwise
"no".
- `size:` snapshot size in bytes.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-fetching-metadata-snapshots-file-system-subvolume](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-fetching-metadata-snapshots-file-system-subvolume)*