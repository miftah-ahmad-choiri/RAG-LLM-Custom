# Viewing information about a CephFS volume

> **Index:** `20.10.1.3`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=volumes-viewing-information-about-cephfs-volume](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=volumes-viewing-information-about-cephfs-volume)

---

## Viewing information about a CephFS volume

Last Updated
: 2026-06-25

List basic details about a Ceph File System (CephFS) volume, such as attributes of data
and metadata pools of the CephFS volume, pending subvolumes deletion count, and the
like.

### Before you begin

Be sure that you have a CephFS volume.

### Procedure

View information about a CephFS volume.

```plaintext-ibm
ceph fs volume info VOLUME_NAME
```

Copy to clipboard

For example:

```
[ceph: root@host01 /]# ceph fs volume info cephfs
{
    "mon_addrs": [
        "192.168.1.7:40977",
    ],
    "pending_subvolume_deletions": 0,
    "pools": {
        "data": [
            {
                "avail": 106288709632,
                "name": "cephfs.cephfs.data",
                "used": 4096
            }
        ],
        "metadata": [
            {
                "avail": 106288709632,
                "name": "cephfs.cephfs.meta",
                "used": 155648
            }
        ]
    },
    "used_size": 0
}
```

The output of the
ceph fs volume info
command includes:
`mon_addrs`
List of monitor addresses.
`pending_subvolume_deletions`
Number of subvolumes pending deletion.
`pools`
Attributes of data and metadata pools.
`avail`
The amount of free space available in bytes.
`name`
Name of the pool.
`used`
The amount of storage consumed in bytes.
`used_size`
Current used size of the CephFS volume in bytes.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=volumes-viewing-information-about-cephfs-volume](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=volumes-viewing-information-about-cephfs-volume)*