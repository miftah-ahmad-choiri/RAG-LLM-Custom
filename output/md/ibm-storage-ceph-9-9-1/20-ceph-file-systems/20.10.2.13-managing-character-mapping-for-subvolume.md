# Managing Character Mapping for subvolume

> **Index:** `20.10.2.13`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-managing-character-mapping-subvolume](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-managing-character-mapping-subvolume)

---

## Managing Character Mapping for subvolume

Last Updated
: 2026-06-25

Learn how to manage character mapping in CephFS.

The CephFS subvolume and subvolumegroup interfaces provide a porcelain API to manage character
mapping `ceph.dir.charmap` settings. These settings support name normalization and
case sensitivity options.

### Configuring Character Mapping

Configure character mapping for subvolume.

To configure character mapping for
subvolume:

```plaintext-ibm
ceph fs subvolume charmap set vol_name subvol group_name=name  setting value
```

Copy to clipboard

### Reading Character Mapping

Read character mapping for subvolume.

To read the character mapping for
subvolume:

```plaintext-ibm
ceph fs subvolume charmap get vol_name subvol group_name=name setting
```

Copy to clipboard

For
example,

```
ceph fs subvolume charmap get vol subvol group_name=csi casesensitive
```

To read the full character mapping for
subvolume:

```plaintext-ibm
ceph fs subvolume charmap get vol_name subvol group_name=name
```

Copy to clipboard

For example,

```
ceph fs subvolumegroup charmap get vol csi
```

Outputs:

```plaintext-ibm
{"casesensitive":false,"normalization":"nfd","encoding":"utf8"}
```

Copy to clipboard

### Removing Character Mapping

Remove character mapping for subvolume.

> 📝 **Note:** Note:
A charmap can only be removed when a subvolume is
empty.

To remove character mapping for
subvolume:

```plaintext-ibm
ceph fs subvolume charmap rm vol_name subvol group_name=name
```

Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-managing-character-mapping-subvolume](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=subvolumes-managing-character-mapping-subvolume)*