# Creating client users for a Ceph File System

> **Index:** `20.9.4`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=cephfs-creating-client-users-ceph-file-system](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=cephfs-creating-client-users-ceph-file-system)

---

## Creating client users for a Ceph File System

Last Updated
: 2026-06-25

IBM Storage Ceph uses `cephx` for
authentication, which is enabled by default. To use `cephx` with the Ceph File
System, create a user with the correct authorization capabilities on a Ceph Monitor node. Also, make
its key available on the node where the Ceph File System will be mounted.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster.
- The Ceph Metadata Server daemon (`ceph-mds`) installed and configured.
- Root-level access to a Ceph Monitor node.
- Root-level access to a Ceph client node.

### About this task

For more information, see
Ceph user management
.

### Procedure

1. Log in to the
`cephadm`
shell on the monitor node.
```plaintext-ibm
cephadm shell
```

Copy to clipboard
2. Create a client user on a Ceph Monitor node.
```plaintext-ibm
ceph fs authorize FILE_SYSTEM_NAME client.CLIENT_NAME /DIRECTORY CAPABILITY [/DIRECTORY CAPABILITY] PERMISSIONS ...
```

Copy to clipboard
Example for restricting the client to only writing in the
temp
directory of
filesystem
cephfs_a

```
[ceph: root@host01 ~]# ceph fs authorize cephfs_a client.1 / r /temp rw

client.1
  key = AQBSdFhcGZFUDRAAcKhG9Cl2HPiDMMRv4DC43A==
```

Example for completely restricting the client to the
temp
directory, by
removing the root (
`/`
) directory

```
[ceph: root@host01 ~]# ceph fs authorize cephfs_a client.1 /temp rw
```

> 📝 **Note:** Note:
Supplying
`all`
or asterisk (
`*`
) as the file system name grants
access to every file system. Typically, it is necessary to put the asterisk in quotations to protect
it from the shell.
3. Verify the created key.
```plaintext-ibm
ceph auth get client.ID
```

Copy to clipboard
For example,

```
[ceph: root@host01 ~]# ceph auth get client.1

client.1
  key = AQBSdFhcGZFUDRAAcKhG9Cl2HPiDMMRv4DC43A==
  caps mds = "allow r, allow rw path=/temp"
  caps mon = "allow r"
  caps osd = "allow rw tag cephfs data=cephfs_a"
```
4. Copy the keyring to the client.
1. On the Ceph Monitor node, export the keyring to a file.
```plaintext-ibm
ceph auth get client.ID -o ceph.client.ID.keyring
```

Copy to clipboard
For
example,

```
[ceph: root@host01 ~]# ceph auth get client.1 -o ceph.client.1.keyring
exported keyring for client.1
```
2. Copy the client keyring from the Ceph Monitor node to the
/etc/ceph/
directory on the client node.Replace
CLIENT_NODE_NAME
with the Ceph client node name or IP.For
example,

```
[ceph: root@host01 ~]# scp /ceph.client.1.keyring root@client01:/etc/ceph/ceph.client.1.keyring
```

  1. On the Ceph Monitor node, export the keyring to a file.
  ```plaintext-ibm
  ceph auth get client.ID -o ceph.client.ID.keyring
  ```



  Copy to clipboard
  For
  example,

  ```
  [ceph: root@host01 ~]# ceph auth get client.1 -o ceph.client.1.keyring
  exported keyring for client.1
  ```
  2. Copy the client keyring from the Ceph Monitor node to the
  /etc/ceph/
  directory on the client node.Replace
  CLIENT_NODE_NAME
  with the Ceph client node name or IP.For
  example,

  ```
  [ceph: root@host01 ~]# scp /ceph.client.1.keyring root@client01:/etc/ceph/ceph.client.1.keyring
  ```
5. From the client node, set the appropriate permissions for the keyring file.
```plaintext-ibm
chmod 644 ceph.client.ID.keyring
```

Copy to clipboard
For
example,

```
[root@client01 ~]# chmod 644 /etc/ceph/ceph.client.1.keyring
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=cephfs-creating-client-users-ceph-file-system](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=cephfs-creating-client-users-ceph-file-system)*