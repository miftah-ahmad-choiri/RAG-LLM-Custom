# Setting file and directory layout fields

> **Index:** `20.15.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=layouts-setting-file-directory-layout-fields](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=layouts-setting-file-directory-layout-fields)

---

## Setting file and directory layout fields

Last Updated
: 2026-06-25

Use the setfattr command to set layout fields on a file or
directory.

### About this task

> 📝 **Note:** Important:
When you modify the layout fields of a file, the file must be empty,
otherwise an error occurs.

For more information, see the
`setfattr(1)`
manual
page.

### Procedure

Modify layout fields on a file or directory.

```plaintext-ibm
setfattr -n ceph.TYPE.layout.FIELD -v VALUE PATH
```

Copy to clipboard

Replace:

- TYPE with `file` or `dir`.
- FIELD with the name of the field.
- VALUE with the new value of the field.
- PATH with the path to the file or directory.

For
example:

```
[root@mon ~]# setfattr -n ceph.file.layout.stripe_unit -v 1048576 test
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=layouts-setting-file-directory-layout-fields](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=layouts-setting-file-directory-layout-fields)*