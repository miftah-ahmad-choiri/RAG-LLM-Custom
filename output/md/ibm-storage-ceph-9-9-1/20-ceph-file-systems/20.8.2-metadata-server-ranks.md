# Metadata Server ranks

> **Index:** `20.8.2`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=server-metadata-ranks](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=server-metadata-ranks)

---

## Metadata Server ranks

Last Updated
: 2026-06-25

Ranks define how the metadata workload is shared between multiple Metadata Server (MDS)
daemons. The number of ranks is the maximum number of MDS daemons that can be active at one time.
Each MDS daemon handles a subset of the CephFS metadata that is assigned to that rank.

Each Ceph File System (CephFS) has a number of ranks, one by default, which starts at zero.

Each MDS daemon initially starts without a rank. The Ceph Monitor assigns a rank to the daemon.
The MDS daemon can only hold one rank at a time. Daemons only lose ranks when they are stopped.

The `max_mds` setting controls how many ranks will be created.

The actual number of ranks in the CephFS is only increased if a spare daemon is available to
accept the new rank.

### Rank states

Ranks can be as follows:

- Up: A rank that is assigned to the MDS daemon.
- Failed: A rank that is not associated with any MDS daemon.
- Damaged: A rank that is damaged; its metadata is corrupted or missing. Damaged
ranks are not assigned to any MDS daemons until the operator fixes the problem, and uses the
ceph mds repaired command on the damaged rank.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=server-metadata-ranks](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=server-metadata-ranks)*