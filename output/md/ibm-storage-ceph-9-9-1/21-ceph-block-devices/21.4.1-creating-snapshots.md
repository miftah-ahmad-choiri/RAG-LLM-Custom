# Creating snapshots

> **Index:** `21.4.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=snapshots-creating](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=snapshots-creating)

---

## Creating snapshots

Last Updated
: 2026-06-25

Create a snapshot of a Ceph Block Device.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Root-level access to the node.

### Procedure

1. Specify the `snap create` option, the pool name, and the image name:

- Method 1:

Syntax

```plaintext-ibm
rbd --pool POOL_NAME snap create --snap SNAP_NAME IMAGE_NAME
```

Copy to clipboard

Example

```plaintext-ibm
[root@rbd-client ~]# rbd --pool pool1 snap create --snap snap1 image1
```

Copy to clipboard
- Method 2:

Syntax

```plaintext-ibm
rbd snap create POOL_NAME/IMAGE_NAME@SNAP_NAME
```

Copy to clipboard

Example

```plaintext-ibm
[root@rbd-client ~]# rbd snap create pool1/image1@snap1
```

Copy to clipboard

  - Method 1:


  Syntax


  ```plaintext-ibm
  rbd --pool POOL_NAME snap create --snap SNAP_NAME IMAGE_NAME
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  [root@rbd-client ~]# rbd --pool pool1 snap create --snap snap1 image1
  ```



  Copy to clipboard
  - Method 2:


  Syntax


  ```plaintext-ibm
  rbd snap create POOL_NAME/IMAGE_NAME@SNAP_NAME
  ```



  Copy to clipboard


  Example


  ```plaintext-ibm
  [root@rbd-client ~]# rbd snap create pool1/image1@snap1
  ```



  Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=snapshots-creating](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=snapshots-creating)*