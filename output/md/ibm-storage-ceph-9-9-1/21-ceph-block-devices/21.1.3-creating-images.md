# Creating images

> **Index:** `21.1.3`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=devices-creating-images](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=devices-creating-images)

---

## Creating images

Last Updated
: 2026-09-06

Before adding a block device to a node, create a Ceph Block Device image for it in the Ceph storage cluster.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster.
- Root-level access to the client node.

### Procedure

Create a block device image.

> 📝 **Note:** Note:
Ensure the pool exists before creating an image.

```plaintext-ibm
rbd create IMAGE_NAME --size MEGABYTES --pool POOL_NAME
```

Copy to clipboard

For example,

```
[root@rbd-client ~]# rbd create image1 --size 1024 --pool pool1
```

This example creates a 1 GB image named `image1` that stores information in a pool named `pool1`.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=devices-creating-images](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=devices-creating-images)*