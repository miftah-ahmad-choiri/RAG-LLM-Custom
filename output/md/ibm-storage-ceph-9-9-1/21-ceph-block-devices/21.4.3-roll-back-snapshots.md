# Roll back snapshots

> **Index:** `21.4.3`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=snapshots-roll-back](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=snapshots-roll-back)

---

## Roll back snapshots

Last Updated
: 2026-06-25

Learn how to rollback a block device snapshot.

> 📝 **Note:** Note:
Rolling back an image to a snapshot means overwriting the current version of the image with
data from a snapshot. The time it takes to run a rollback increases with the size of the image. It
is
**faster to clone**
from a snapshot
**than to rollback**
an image to a snapshot, and it is
the preferred method of returning to a pre-existing state.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Root-level access to the node.

### Procedure

1. Specify the `snap rollback` option, the pool name, the image name and the snap
name:

Syntax

```plaintext-ibm
rbd --pool POOL_NAME snap rollback --snap SNAP_NAME IMAGE_NAME
rbd snap rollback POOL_NAME/IMAGE_NAME@SNAP_NAME
```

Copy to clipboard

Example

```plaintext-ibm
[root@rbd-client ~]# rbd --pool pool1 snap rollback --snap snap1 image1
[root@rbd-client ~]# rbd snap rollback pool1/image1@snap1
```

Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=snapshots-roll-back](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=snapshots-roll-back)*