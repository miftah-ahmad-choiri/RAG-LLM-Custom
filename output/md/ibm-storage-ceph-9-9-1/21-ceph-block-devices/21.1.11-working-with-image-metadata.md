# Working with image metadata

> **Index:** `21.1.11`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=devices-working-image-metadata](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=devices-working-image-metadata)

---

## Working with image metadata

Last Updated
: 2026-06-25

Ceph supports adding custom image metadata as key-value pairs. The pairs do not have any
strict format. By using metadata you can also set the RADOS Block Device (RBD) configuration
parameters for particular images. Use the rbd image-meta command to work with
metadata.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Root-level access to the client node.

### Procedure

1. To set a new metadata key-value pair:

Syntax

```plaintext-ibm
rbd image-meta set POOL_NAME/IMAGE_NAME KEY VALUE
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# rbd image-meta set pool1/image1 last_update 2021-06-06
```

Copy to clipboard

This example sets the `last_update` key to the `2021-06-06` value
on the `image1` image in the `pool1` pool.
2. To view a value of a key:

Syntax

```plaintext-ibm
rbd image-meta get POOL_NAME/IMAGE_NAME KEY
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# rbd image-meta get pool1/image1 last_update
```

Copy to clipboard

This example views the value of the `last_update` key.
3. To show all metadata on an image:

Syntax

```plaintext-ibm
rbd image-meta list POOL_NAME/IMAGE_NAME
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# rbd image-meta list pool1/image1
```

Copy to clipboard

This example lists the metadata set for the `image1` image in the
`pool1` pool.
4. To remove a metadata key-value pair:

Syntax

```plaintext-ibm
rbd image-meta remove POOL_NAME/IMAGE_NAME KEY
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# rbd image-meta remove pool1/image1 last_update
```

Copy to clipboard

This example removes the `last_update` key-value pair from the
`image1` image in the `pool1` pool.
5. To override the RBD image configuration settings set in the Ceph configuration file for a
particular image:

Syntax

```plaintext-ibm
rbd config image set POOL_NAME/IMAGE_NAME  PARAMETER VALUE
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# rbd config image set pool1/image1 rbd_cache false
```

Copy to clipboard

This example disables the RBD cache for the `image1` image in the
`pool1` pool.

### Reference

For a list of possible configuration options, see General options.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=devices-working-image-metadata](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=devices-working-image-metadata)*