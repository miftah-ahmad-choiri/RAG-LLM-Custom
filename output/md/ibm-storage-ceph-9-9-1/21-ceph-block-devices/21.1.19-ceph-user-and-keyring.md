# Ceph user and keyring

> **Index:** `21.1.19`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=devices-ceph-user-keyring](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=devices-ceph-user-keyring)

---

## Ceph user and keyring

Last Updated
: 2026-06-25

When `cephx` is enabled, you must specify a user name or ID and a path to
the keyring containing the corresponding key for the user.

> 📝 **Note:** Note:
`cephx`
is enabled by default.

You might also add the `CEPH_ARGS` environment variable to avoid re-entry of the
following parameters:

Syntax

```plaintext-ibm
rbd --id USER_ID --keyring=/path/to/secret [commands]
rbd --name USERNAME --keyring=/path/to/secret [commands]
```

Copy to clipboard

Example

```plaintext-ibm
[root@rbd-client ~]# rbd --id admin --keyring=/etc/ceph/ceph.keyring [commands]
[root@rbd-client ~]# rbd --name client.admin --keyring=/etc/ceph/ceph.keyring [commands]
```

Copy to clipboard

> 📝 **Note:** Tip:
Add the user and secret to the
`CEPH_ARGS`
environment variable so
that you do not need to enter them each time.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=devices-ceph-user-keyring](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=devices-ceph-user-keyring)*