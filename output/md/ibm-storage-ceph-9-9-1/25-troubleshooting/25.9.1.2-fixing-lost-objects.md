# Fixing lost objects

> **Index:** `25.9.1.2`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-fixing-lost-objects](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-fixing-lost-objects)

---

## Fixing lost objects

Last Updated
: 2026-06-25

Use the `ceph-objectstore-tool` utility to list and fix lost
and unfound legacy objects that are stored within a Ceph OSD.

### Before you begin

- Root-level access to the Ceph OSD node.
- Stopping the `ceph-osd` daemon.

### About this task

> 📝 **Note:** Note:
This procedure applies only to legacy objects.

### Procedure

1. Verify that the appropriate OSD is down.
```plaintext-ibm
systemctl status ceph-osd@OSD_NUMBER
```

Copy to clipboard
For
example,

```
[root@host01 ~]# systemctl status ceph-osd@1
```
2. Log in to the OSD container.
```plaintext-ibm
cephadm shell --name osd.OSD_ID
```

Copy to clipboard
For
example,

```
[root@host01 ~]# cephadm shell --name osd.0
```
3. List all lost legacy objects.
```plaintext-ibm
ceph-objectstore-tool --data-path PATH_TO_OSD --op fix-lost --dry-run
```

Copy to clipboard
For
example,

```
[ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 --op fix-lost --dry-run
```
4. Fix the
lost
and
unfound
objects by using the
`ceph-objectstore-tool`
utility.Use the following procedures, depending
on the circumstance.
- Fix all lost
objects.
```plaintext-ibm
ceph-objectstore-tool --data-path PATH_TO_OSD --op fix-lost
```

Copy to clipboard
For
example,
```
[ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 --op fix-lost
```
- Fix all lost objects within a placement
group.
```plaintext-ibm
ceph-objectstore-tool --data-path PATH_TO_OSD --pgid PG_ID --op fix-lost
```

Copy to clipboard
For
example,
```
[ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 --pgid 0.1c --op fix-lost
```
- Fix a lost object by its
identifier.
```plaintext-ibm
ceph-objectstore-tool --data-path PATH_TO_OSD --op fix-lost OBJECT_ID
```

Copy to clipboard
For
example,
```
[ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 --op fix-lost default.region
```

  - Fix all lost
  objects.
  ```plaintext-ibm
  ceph-objectstore-tool --data-path PATH_TO_OSD --op fix-lost
  ```



  Copy to clipboard
  For
  example,
  ```
  [ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 --op fix-lost
  ```
  - Fix all lost objects within a placement
  group.
  ```plaintext-ibm
  ceph-objectstore-tool --data-path PATH_TO_OSD --pgid PG_ID --op fix-lost
  ```



  Copy to clipboard
  For
  example,
  ```
  [ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 --pgid 0.1c --op fix-lost
  ```
  - Fix a lost object by its
  identifier.
  ```plaintext-ibm
  ceph-objectstore-tool --data-path PATH_TO_OSD --op fix-lost OBJECT_ID
  ```



  Copy to clipboard
  For
  example,
  ```
  [ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 --op fix-lost default.region
  ```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-fixing-lost-objects](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-fixing-lost-objects)*