# Manipulating object attribute key

> **Index:** `25.9.2.7`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-manipulating-object-attribute-key](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-manipulating-object-attribute-key)

---

## Manipulating object attribute key

Last Updated
: 2026-06-25

Use the `ceph-objectstore-tool` utility to change object
attributes.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- Root-level access to the Ceph OSD node.
- Stop the `ceph-osd` daemon.

### About this task

To manipulate object attributes you need the data paths, the placement group identifier (PG ID),
the object, and the key in the object attribute.

### Procedure

1. Verify that the appropriate OSD is down.
```plaintext-ibm
systemctl status ceph-osd@OSD_ID
```

Copy to clipboard
For
example,

```
[root@host01 ~]# systemctl status ceph-osd@1
```
2. Log in to the OSD container.
```plaintext-ibm
cephadm shell --name osd.OSD_ID
```

Copy to clipboard
For
example,

```
[root@host01 ~]# cephadm shell --name osd.0
```
3. Get the attributes of the object.
```plaintext-ibm
ceph-objectstore-tool --data-path PATH_TO_OSD \
--pgid PG_ID OBJECT \
get-attr KEY > OBJECT_ATTRS_FILE_NAME
```

Copy to clipboard
For
example,

```
[ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 \
--pgid 0.1c '{"oid":"zone_info.default","key":"","snapid":-2,"hash":235010478,"max":0,"pool":11,"namespace":""}' \
get-attr "oid" > zone_info.default.attr.txt
```
4. Set the attributes of the object.
```plaintext-ibm
ceph-objectstore-tool --data-path PATH_TO_OSD \
--pgid PG_ID OBJECT \
set-attr KEY > OBJECT_ATTRS_FILE_NAME
```

Copy to clipboard
For
example,

```
[ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 \
--pgid 0.1c '{"oid":"zone_info.default","key":"","snapid":-2,"hash":235010478,"max":0,"pool":11,"namespace":""}' \
set-attr "oid" > zone_info.default.attr.txt
```
5. Remove the attributes of the object.
```plaintext-ibm
ceph-objectstore-tool --data-path PATH_TO_OSD \
--pgid PG_ID OBJECT  \
rm-attr KEY
```

Copy to clipboard
For example,

```
[ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 \
--pgid 0.1c '{"oid":"zone_info.default","key":"","snapid":-2,"hash":235010478,"max":0,"pool":11,"namespace":""}' \
rm-attr "oid"
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-manipulating-object-attribute-key](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-manipulating-object-attribute-key)*