# Listing object attributes

> **Index:** `25.9.2.6`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-listing-object-attributes](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-listing-object-attributes)

---

## Listing object attributes

Last Updated
: 2026-06-25

Use the `ceph-objectstore-tool` utility to list object
attributes.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- Root-level access to the Ceph OSD node.
- Stopping the `ceph-osd` daemon.

### About this task

The output provides you with the object’s keys and values.

### Procedure

1. Verify that the appropriate OSD is down.
```plaintext-ibm
systemctl status ceph-osd@OSD_ID
```

Copy to clipboard
For
example,

```
[root@host01 ~]# systemctl status ceph-osd@1
```
2. Log in to the OSD container.
```plaintext-ibm
cephadm shell --name osd.OSD_ID
```

Copy to clipboard
For
example,

```
[root@host01 ~]# cephadm shell --name osd.0
```
3. List the object attributes.
```plaintext-ibm
ceph-objectstore-tool --data-path PATH_TO_OSD \
--pgid PG_ID OBJECT \
list-attrs
```

Copy to clipboard
For
example,

```
[ceph: root@host01 /]# ceph-objectstore-tool --data-path /var/lib/ceph/osd/ceph-0 \
--pgid 0.1c '{"oid":"zone_info.default","key":"","snapid":-2,"hash":235010478,"max":0,"pool":11,"namespace":""}' \
list-attrs
```

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-listing-object-attributes](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=operations-listing-object-attributes)*