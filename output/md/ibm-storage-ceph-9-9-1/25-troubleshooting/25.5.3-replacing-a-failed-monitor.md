# Replacing a failed Monitor

> **Index:** `25.5.3`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=monitors-replacing-failed-monitor](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=monitors-replacing-failed-monitor)

---

## Replacing a failed Monitor

Last Updated
: 2026-06-25

When a Ceph Monitor has a corrupted store, replace the Monitor in the storage
cluster.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster
- Able to form a quorum
- Root-level access to Ceph Monitor node

### About this task

For more information, see
Ceph Monitor is out of quorum
.

### Procedure

1. From the Monitor host, remove the Monitor store.The Monitor store is located,
by default, in
/var/lib/ceph/mon/
CLUSTER_NAME-SHORT_HOST_NAME
.

```plaintext-ibm
rm -rf /var/lib/ceph/mon/CLUSTER_NAME-SHORT_HOST_NAME
```

Copy to clipboard

Specify
the short hostname of the Monitor host and the cluster name.For example, to remove
the Monitor store of a Monitor running on
`host1`
from a cluster called
`remote`
, run the following
command:

```
[root@mon ~]# rm -rf /var/lib/ceph/mon/remote-host1
```
2. Remove the Monitor from the Monitor map
(
`monmap`
).
```plaintext-ibm
ceph mon remove SHORT_HOST_NAME --cluster CLUSTER_NAME
```

Copy to clipboard

Specify
the short hostname of the Monitor host and the cluster name.For example, to remove
the Monitor running on
`host1`
from a cluster called
`remote`
, run the
following
command:

```
[ceph: root@host01 /]# ceph mon remove host01 --cluster remote
```
3. Troubleshoot and fix any problems that are related to the underlying file system or
hardware of the Monitor host.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=monitors-replacing-failed-monitor](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=monitors-replacing-failed-monitor)*