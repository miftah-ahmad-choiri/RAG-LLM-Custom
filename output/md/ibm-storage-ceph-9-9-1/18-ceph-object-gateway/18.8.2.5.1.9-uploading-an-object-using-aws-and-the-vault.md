# Uploading an object using AWS and the Vault

> **Index:** `18.8.2.5.1.9`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=vault-uploading-object-using-aws](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=vault-uploading-object-using-aws)

---

## Uploading an object using AWS and the Vault

Last Updated
: 2026-06-25

When uploading an object to the Ceph Object Gateway, the Ceph Object Gateway fetches the
key from the Vault, and then encrypts and stores the object in a bucket. When a request is made to
download the object, the Ceph Object Gateway automatically retrieves the corresponding key from the
Vault and decrypts the object. To upload an object, the Ceph Object Gateway fetches the key from the
Vault and then encrypts the object and stores it in the bucket. The Ceph Object Gateway retrieves
the corresponding key from the Vault and decrypts the object when there is a request to download the
object.

> 📝 **Note:** Note:
The URL is constructed using the base address, set by the
`rgw_crypt_vault_addr`
option, and the path prefix, set by the
`rgw_crypt_vault_prefix`
option.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Installation of the Ceph Object Gateway software.
- Installation of the HashiCorp Vault software.
- Access to a Ceph Object Gateway client node.
- Access to Amazon Web Services (AWS).

### Procedure

1. Upload an object using the AWS command line client and provide the Secure Side Encryption(SSE)
key ID in the request:

1. For the Key/Value secret engine:

Example

```plaintext-ibm
[user@client ~]$ aws --endpoint=http://radosgw:8000 s3 cp plaintext.txt s3://mybucket/encrypted.txt --sse=aws:kms --sse-kms-key-id myproject/mybucketkey
```

Copy to clipboard

> 📝 **Note:** Note:
In the example, the Ceph Object Gateway would fetch the secret from
`http://vault-server:8200/v1/secret/data/myproject/mybucketkey`
2. For the Transit engine:

Example

```plaintext-ibm
[user@client ~]$ aws --endpoint=http://radosgw:8000 s3 cp plaintext.txt s3://mybucket/encrypted.txt --sse=aws:kms --sse-kms-key-id mybucketkey
```

Copy to clipboard

  1. For the Key/Value secret engine:


  Example


  ```plaintext-ibm
  [user@client ~]$ aws --endpoint=http://radosgw:8000 s3 cp plaintext.txt s3://mybucket/encrypted.txt --sse=aws:kms --sse-kms-key-id myproject/mybucketkey
  ```



  Copy to clipboard


  > 📝 **Note:** Note:
  In the example, the Ceph Object Gateway would fetch the secret from
  `http://vault-server:8200/v1/secret/data/myproject/mybucketkey`
  2. For the Transit engine:


  Example


  ```plaintext-ibm
  [user@client ~]$ aws --endpoint=http://radosgw:8000 s3 cp plaintext.txt s3://mybucket/encrypted.txt --sse=aws:kms --sse-kms-key-id mybucketkey
  ```



  Copy to clipboard

> 📝 **Note:** Note:
In the example, the Ceph Object Gateway would fetch the secret from
`http://vaultserver:8200/v1/transit/mybucketkey`

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=vault-uploading-object-using-aws](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=vault-uploading-object-using-aws)*