# Creating a lifecycle management policy

> **Index:** `18.4.1.7.1`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=lifecycle-creating-management-policy](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=lifecycle-creating-management-policy)

---

## Creating a lifecycle management policy

Last Updated
: 2026-06-25

You can manage a bucket lifecycle policy configuration using standard S3 operations
rather than using the radosgw-admin command. RADOS Gateway supports only a subset
of the Amazon S3 API policy language applied to buckets. The lifecycle configuration contains one or
more rules defined for a set of bucket objects.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Installation of the Ceph Object Gateway.
- Root-level access to a Ceph Object Gateway node.
- An S3 bucket created.
- An S3 user created with user access.
- Access to a Ceph Object Gateway client with the `AWS CLI` package installed.

### Procedure

1. Create a JSON file for lifecycle configuration:

Example

```plaintext-ibm
[user@client ~]$ vi lifecycle.json
```

Copy to clipboard
2. Add the specific lifecycle configuration rules in the file:

Example

```plaintext-ibm
{
        "Rules": [
        {
                    "Filter": {
                            "Prefix": "images/"
                    },
                    "Status": "Enabled",
                    "Expiration": {
                            "Days": 1
                    },
                    "ID": "ImageExpiration"
            }
    ]
}
```

Copy to clipboard

The lifecycle configuration example expires objects in the images directory after 1 day.
3. Set the lifecycle configuration on the bucket:

Syntax

```plaintext-ibm
aws --endpoint-url=_RADOSGW_ENDPOINT_URL_:PORT s3api put-bucket-lifecycle-configuration --bucket _BUCKET_NAME_ --lifecycle-configuration file://_PATH_TO_LIFECYCLE_CONFIGURATION_FILE_/_LIFECYCLE_CONFIGURATION_FILE_.json
```

Copy to clipboard

Example

```plaintext-ibm
[user@client ~]$ aws --endpoint-url=http://host01:80 s3api put-bucket-lifecycle-configuration --bucket testbucket --lifecycle-configuration file://lifecycle.json
```

Copy to clipboard

In this example, the `lifecycle.json` file exists in the current directory.

- Retrieve the lifecycle configuration for the bucket:

Syntax

```plaintext-ibm
aws --endpoint-url=_RADOSGW_ENDPOINT_URL_:PORT s3api get-bucket-lifecycle-configuration --bucket _BUCKET_NAME_
```

Copy to clipboard

Example

```plaintext-ibm
[user@client ~]$ aws --endpoint-url=http://host01:80 s3api get-bucket-lifecycle-configuration --bucket testbucket
{
        "Rules": [
        {
                    "Expiration": {
                            "Days": 1
                    },
                    "ID": "ImageExpiration",
                    "Filter": {
                            "Prefix": "images/"
                    },
                    "Status": "Enabled"
            }
    ]
}
```

Copy to clipboard
- Optional: From the Ceph Object Gateway node, log into the `cephadm` shell and retrieve the bucket
lifecycle configuration:

Syntax

```plaintext-ibm
radosgw-admin lc get --bucket=BUCKET_NAME
```

Copy to clipboard

Example

```plaintext-ibm
[ceph: root@host01 /]# radosgw-admin lc get --bucket=testbucket
{
        "prefix_map": {
                "images/": {
                        "status": true,
                        "dm_expiration": false,
                        "expiration": 1,
                        "noncur_expiration": 0,
                        "mp_expiration": 0,
                        "transitions": {},
                        "noncur_transitions": {}
                }
        },
        "rule_map": [
        {
                "id": "ImageExpiration",
                "rule": {
                        "id": "ImageExpiration",
                        "prefix": "",
                        "status": "Enabled",
                        "expiration": {
                                "days": "1",
                                "date": ""
                        },
                        "mp_expiration": {
                                "days": "",
                                "date": ""
                        },
                        "filter": {
                                "prefix": "images/",
                                "obj_tags": {
                                        "tagset": {}
                                }
                        },
                        "transitions": {},
                        "noncur_transitions": {},
                        "dm_expiration": false
                }
        }
  ]
}
```

Copy to clipboard

### Reference

For more information, see the following:

- S3 bucket lifecycle
- [Setting lifecycle configuration on a bucket](https://www.ibm.com/links?url=https%3A%2F%2Fdocs.aws.amazon.com%2FAmazonS3%2Flatest%2Fuserguide%2Fhow-to-set-lifecycle-configuration-intro.html)

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=lifecycle-creating-management-policy](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=lifecycle-creating-management-policy)*