# Viewing Ceph Object Gateway per-user and per-bucket usage statistics (Technology Preview)

> **Index:** `18.10`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=cog-viewing-ceph-object-gateway-per-user-per-bucket-usage-statistics-technology-preview](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=cog-viewing-ceph-object-gateway-per-user-per-bucket-usage-statistics-technology-preview)

---

## Viewing Ceph Object Gateway per-user and per-bucket usage statistics (Technology Preview)

Last Updated
: 2026-06-25

Use the Ceph Object Gateway performance counters to view per-user and per-bucket usage statistics, such as total bytes used and number of objects.Performance counters are available through the Ceph admin socket and use a cache to provide accurate, low-latency metrics with minimal overhead.

### Before you begin

Before you begin, make sure that you have the following prerequisites in place:

- A running IBM Storage Ceph cluster.
- Ceph Object Gateway is installed and running.
- Object gateway login credentials are configured.
- At least one user and one bucket exist and are active (not suspended).
- Configure the following parameters to the Ceph Object Gateway service and then restart the `rgw ` service to apply the changes, by using the ceph orch restart rgw command.


> 📝 **Note:** Note:
This feature is disabled by default. Configuring these parameters enables the per-user and per-bucket view feature.

```plaintext-ibm
ceph config set client.rgw rgw_enable_usage_perf_counters true
ceph config set client.rgw rgw_usage_cache_path /tmp/usage_cache.mdb
ceph config set client.rgw rgw_usage_cache_max_size 1073741824
```

Copy to clipboard

### About this task

> 📝 **Note:** Important:
Technology Preview features are not supported with
IBM
production service level agreements (SLAs), might not be functionally complete, and
IBM
does not recommend using them for production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.

### Procedure

1. Create a user.

```plaintext-ibm
radosgw-admin user create
```

Copy to clipboard

For example,

```
radosgw-admin user create \
  --uid=testuser \
  --display-name="Test User" \
  --access-key=test \
  --secret-key=test
```
2. Configure a S3 client.
For example,

```
[default]
access_key = test
secret_key = test
host_base = localhost:8000
host_bucket = localhost:8000
use_https = False
signature_v2 = True
```
3. Create buckets and upload objects.
For example,

```
[default]
access_key = test
secret_key = test
host_base = localhost:8000
host_bucket = localhost:8000
use_https = False
signature_v2 = True
```
4. View usage counters through the admin socket.

> 📝 **Note:** Note:
Counter statistics are not real time. After uploading objects, counters may take up to 40 minutes to reflect changes.

```plaintext-ibm
SOCKET=out/radosgw.8000.asok
# ceph --admin-daemon $SOCKET perf dump | grep -A 20 rgw_usage
```

Copy to clipboard

Example counters include:

- `rgw_usage` – Global counters
- `rgw_usage_cache` – Cache statistics
- `rgw_user_testuser` – User-specific statistics
- `rgw_bucket_bucket1` – Bucket-specific statistics

Example output for
rgw_user_testuser

```
"used_bytes": 15728640,
"num_objects": 3
```

Example output for
rgw_bucket_bucket1

```
"used_bytes": 5242880,
"num_objects": 2
```

  - `rgw_usage` – Global counters
  - `rgw_usage_cache` – Cache statistics
  - `rgw_user_testuser` – User-specific statistics
  - `rgw_bucket_bucket1` – Bucket-specific statistics
5. Verify cache behavior.

1. List a bucket multiple times.

```plaintext-ibm
s3cmd ls s3://bucket1
s3cmd ls s3://bucket1
s3cmd ls s3://bucket1
```

Copy to clipboard
2. Check the cache hit counter.

```plaintext-ibm
ceph --admin-daemon $SOCKET perf dump | grep cache_hit
```

Copy to clipboard

  1. List a bucket multiple times.

  ```plaintext-ibm
  s3cmd ls s3://bucket1
  s3cmd ls s3://bucket1
  s3cmd ls s3://bucket1
  ```



  Copy to clipboard
  2. Check the cache hit counter.

  ```plaintext-ibm
  ceph --admin-daemon $SOCKET perf dump | grep cache_hit
  ```



  Copy to clipboard

### Results

You can now view
`per-user`
and
`per-bucket usage`
statistics for Ceph Object Gateway, confirm that usage counters are active, and verify cache behavior.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=cog-viewing-ceph-object-gateway-per-user-per-bucket-usage-statistics-technology-preview](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=cog-viewing-ceph-object-gateway-per-user-per-bucket-usage-statistics-technology-preview)*