# Using a custom search filter

> **Index:** `18.7.1.7`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=integration-using-custom-search-filter](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=integration-using-custom-search-filter)

---

## Using a custom search filter

Last Updated
: 2026-06-25

You can create a custom search filter to limit user access by using the
`rgw_ldap_searchfilter` setting.

There are two ways to use the `rgw_ldap_searchfilter` setting:

1. Specifying a partial filter:

Example

```plaintext-ibm
"objectclass=inetorgperson"
```

Copy to clipboard

The Ceph Object Gateway generates the search filter with the user name from the token and the
value of `rgw_ldap_dnattr`. The constructed filter is then combined with the partial
filter from the `rgw_ldap_searchfilter` value. For example, the user name and the
settings generate the final search filter:

Example

```plaintext-ibm
"(&(uid=joe)(objectclass=inetorgperson))"
```

Copy to clipboard

User `joe` is only granted access if he is found in the LDAP directory, has an
object class of `inetorgperson`, and specifies a valid password.
2. Specifying a complete filter:

A complete filter must contain a `USERNAME` token which is substituted with the
user name during the authentication attempt. The `rgw_ldap_dnattr` setting is not
used in this case. For example, to limit valid users to a specific group, use the following
filter:

Example

```plaintext-ibm
"(&(uid=@USERNAME@)(memberOf=cn=ceph-users,ou=groups,dc=mycompany,dc=com))"
```

Copy to clipboard

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=integration-using-custom-search-filter](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=integration-using-custom-search-filter)*