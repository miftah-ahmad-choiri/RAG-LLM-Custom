# Resynchronizing a multi-factor authentication TOTP token

> **Index:** `18.8.3.4`  
> **Source:** [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=authentication-resynchronizing-multi-factor-totp-token](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=authentication-resynchronizing-multi-factor-totp-token)

---

## Resynchronizing a multi-factor authentication TOTP token

Last Updated
: 2026-06-25

Resynchronize a multi-factor authentication (MFA) time-based one time password
token.

### Prerequisites

- A running IBM Storage Ceph cluster.
- Ceph Object Gateway is installed.
- You have root access on a Ceph Monitor node.
- An MFA TOTP token was created using `radosgw-admin mfa create`.

### Procedure

1. Resynchronize a multi-factor authentication TOTP token in case of time skew or failed checks.

This requires passing in two consecutive pins: the previous pin, and the current pin.

Syntax

```plaintext-ibm
radosgw-admin mfa resync --uid=USERID --totp-serial=SERIAL --totp-pin=PREVIOUS_PIN --totp=pin=CURRENT_PIN
```

Copy to clipboard

Set *USERID* to the user name MFA is set up on, set *SERIAL* to the string that
represents the ID for the TOTP token, set *PREVIOUS_PIN* to the user’s previous PIN, and set
*CURRENT_PIN* to the user’s current PIN.

Example

```plaintext-ibm
[root@host01 ~]# radosgw-admin mfa resync --uid=johndoe --totp-serial=MFAtest --totp-pin=802021 --totp-pin=439996
```

Copy to clipboard
2. Verify the token was successfully resynchronized by testing a new PIN:

Syntax

```plaintext-ibm
radosgw-admin mfa check --uid=USERID --totp-serial=SERIAL --totp-pin=PIN
```

Copy to clipboard

Set *USERID* to the user name MFA is set up on, set *SERIAL* to the string that
represents the ID for the TOTP token, and set *PIN* to the user’s PIN.

Example

```plaintext-ibm
[root@host01 ~]# radosgw-admin mfa check  --uid=johndoe --totp-serial=MFAtest --totp-pin=870305
ok
```

Copy to clipboard

### Reference

For more information, see Creating a seed for multi-factor authentication.

---

*Source: [https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=authentication-resynchronizing-multi-factor-totp-token](https://www.ibm.com/docs/en/storage-ceph/9.9.1?topic=authentication-resynchronizing-multi-factor-totp-token)*